# Genesis CMS — Tarea actual

> **📖 Ver `docs/context/HOME_INTEGRATION.md` antes de tocar el backend de cualquier bloque del Home** (schemas de `PageResource.php`, `PropertiesSchema`, `ResolvesPublicLinks`, módulos de Testimonios/Servicios/Logos/Menú) — resumen curado de dónde vive cada pieza, los patrones ya establecidos y el historial de bugs ya resueltos (theme de Filament, componentes `<x-filament::input.*>`, superficies de docs de API a mantener sincronizadas). El Home de CICA360 (Cliente 0) quedó confirmado por el Tech Lead 2026-09-02 como fiel al diseño de Figma.
>
> Última actualización: 2026-09-18 (**Nuevo widget "Últimos cambios" en el Dashboard (10 contenidos más recientes)** — pedido del Tech Lead: "crear un widgets con ultimos cambios, ya sea en contenidos por tipo pagina, legales, secciones, y blog, servicios, testimonios, pero mostrar los 10 ultimos contenidos que fueron actualizados", con aclaración inmediata de alcance: "que tengan acceso los roles de marketing, editores y redactores". Nueva clase `RecentContentChangesWidget` (`Widget` + vista propia, no `TableWidget` — la fuente son 4 modelos Eloquent distintos: `Page`, `Post`, `Service`, `Testimonial`, imposible de expresar como un único `Builder`). Trae hasta 10 de cada tipo (tenant-scoped, acotado por la Policy real de cada modelo vía `can('viewAny', X::class)`), une y se queda con el top 10 por `updated_at` desc. `Testimonial` no tiene `title` (usa `name`) — mapeado explícitamente para tratar los 4 tipos de forma uniforme en la vista. `canView()`: visible si hay tenant y el usuario tiene `viewAny` de al menos uno de los 4 modelos — con la matriz de 5 roles vigente esto es visible para los 5 roles (a propósito, sin la restricción de los widgets de Contactos). Cada item: título, badge de tipo con color e ícono propios (reusa el `navigationIcon` de cada Resource), fecha amigable (`FriendlyDate`), link a la pantalla índice del Resource dueño. Registrado en `PanelCmsProvider.php` con `$sort = -15` (entre `PlanUsageWidget` y `RecentContactsWidget`, misma fila de la grilla). Ver PROGRESS.md. **Pendiente: confirmación visual en vivo del Tech Lead (orden, colores, links) y `php artisan test`/`pint` cuando haya entorno disponible. Commit pendiente.** Owner: libre.)
> Antes, mismo día (**Fix real: widgets de contactos del Dashboard (Leads/Últimos contactos) visibles sin acceso a Contactos/Formularios** — pedido del Tech Lead viendo el Escritorio con un rol restringido: "si el rol no tiene acceso a contactos o formularios, no debería... acceso a los 5 widgets" (4 KPIs de `LeadsOverviewWidget` + tabla de `RecentContactsWidget`). Ninguno de los 2 `canView()` chequeaba Policy alguna, solo que hubiera tenant — cualquier rol, incluido `Author`, veía datos reales de leads. Fix: `canView()` ahora exige `$user->can('viewAny', Contact::class) && $user->can('viewAny', Form::class)` (AMBOS, pedido explícito) — delega a `ContactPolicy`/`FormPolicy` en vez de listas de roles a mano. Con la matriz de 5 roles esto oculta ambos widgets a `Author` Y a `Editor` (tiene Contactos pero no Formularios) — `Editor` conserva su acceso normal a `ContactResource`, solo se oculta el resumen del Dashboard. Ver PROGRESS.md. **Pendiente: confirmación en vivo con Editor/Author/Marketing. Commit pendiente.** Owner: libre.)
> Antes, mismo día (**2do fix real (mismo hilo): 404 de ruta no coincidente en Studio seguía sin redirigir — faltaba `Route::fallback()` con sesión disponible** — el Tech Lead probó el fix del 403 y reportó que una URL de Studio que no matchea ninguna ruta (`/cica360/preferencias`, mal escrito) seguía mostrando "404 | Not Found" crudo. Causa: el middleware del panel `cms` (`StartSession`/`FilamentAuthenticate`) está atado a las RUTAS que Filament registra — si el router no encuentra ninguna, esos middlewares nunca corren, no hay sesión, y `auth()->user()` dentro del exception handler de `bootstrap/app.php` siempre da `null` (el 403 SÍ funciona porque esa ruta matchea y sus middlewares sí corren). Fix real: `Route::fallback()` nuevo en `routes/web.php` (scoped al dominio de Studio) — al vivir en `routes/web.php`, Laravel le aplica el grupo `web` con `StartSession` automáticamente, así que ahí la sesión sí está disponible. Redirige a un usuario autenticado a su Escritorio/Platform, a un guest a `Panel::getLoginUrl()` (no `route('login')` — esa ruta no existe con ese nombre literal en esta app, confirmado en el código fuente de Filament). Ver PROGRESS.md. **Pendiente: confirmación en vivo con distintos roles y como guest. Commit pendiente.** Owner: libre.)
> Antes, mismo día (**Fix real: link "Preferencias" visible sin acceso en el dropdown de perfil + 403 crudo de Laravel en vez de redirigir al Escritorio** — 3 síntomas reportados por el Tech Lead con captura, probando con una cuenta `Marketing`: (1) el link "Preferencias" del dropdown de avatar se mostraba aunque ese rol no tenga acceso (`MenuItem` no tenía `->visible()`, a diferencia de "Ir a Platform Manager") → clic daba 403; (2) navegar por URL directa a una ruta sin acceso mostraba el 403 crudo de Laravel en vez de redirigir; (3) un parpadeo de esa misma pantalla en blanco durante el logout, "sea cual sea el rol" — no reproducido de forma aislada en este sandbox (revisados los 3 middlewares que corren en logout, ninguno dispara 403 ahí). Fix 1: `->visible(fn () => Preferences::canAccess())` en `PanelCmsProvider.php`. Fix 2: nuevo `$exceptions->render()` en `bootstrap/app.php` para `AccessDeniedHttpException` en el host de Studio (mismo patrón que el handler ya existente de 404/tenant inválido) — redirige al Escritorio del tenant del usuario autenticado en vez de mostrar el error. Se espera que esto también resuelva el síntoma 3 si su causa era un 403 transitorio, pero queda sin confirmar. Ver PROGRESS.md. **Pendiente: `php artisan test --compact`, `vendor/bin/pint --dirty --format agent`, y confirmación en vivo del Tech Lead de los 3 síntomas, especialmente el logout. Commit pendiente.** Owner: libre.)
> Antes, mismo día (**WelcomeWidget: badge del Escritorio pasa de mostrar el plan a mostrar "Propietario"/"Administrador"/rol** — pedido del Tech Lead con captura: el badge junto al nombre mostraba el plan del tenant, ahora muestra si el usuario logueado es dueño de la cuenta ("Propietario") o solo tiene rol `Admin` ("Administrador") — mismo nivel de acceso real en ambos casos, distinción puramente visual. Nuevo `Tenant::isOwnedBy()` (heurístico: primer usuario creado del tenant, no hay campo `owner_id` en el esquema). `WelcomeWidget::getRoleLabel()`/`getRoleBadgeColor()` reemplazan a los de plan; el plan sigue visible sin cambios en `PlanStatusWidget`. Ver PROGRESS.md. **Pendiente: confirmación visual con un Admin dueño y un Admin agregado después, y con los 5 roles.** Owner: libre.)
> Antes, mismo día (**2do addendum ADR-078: expansión de 3 a 5 roles en Studio (`Admin`/`Soporte`/`Marketing`/`Editor`/`Author`) + `PlanUsageWidget` gated por rol** — pedido del Tech Lead con el sistema de roles de ADR-078 ya funcionando: sumar `Soporte` ("accesos a todos similar al administrador pero excepto a ajustes (usuarios)") y `Marketing` ("parecido al acceso del administrador... pero sin acceso a ajustes, desarrolladores, preferencias"), y ocultar el widget de uso del plan del Dashboard a todos salvo Admin/Soporte. 4 rondas de `AskUserQuestion` para resolver que `Marketing` y el `Editor` ya existente se describían casi idénticos — resuelto: `Marketing` conserva Multimedia/Menús/Sliders (+ Contactos/Formularios), `Editor` los PIERDE y queda acotado a Contenidos/Blog/Servicios/Testimonios + Contactos (sin quitarle Contactos, pedido aparte). `UserRoleEnum` +2 casos; `TenantRolePolicy` (Contenidos) extendida a 5 roles; `EditorManagedPolicy` renombrada a `SupportManagedPolicy` (`[Admin, Soporte, Marketing]`, usada por Media/Menu/Slider/Form); `ContactPolicy` con array propio (única Policy técnica donde Editor conserva acceso); Pages de Desarrolladores/Preferencias de `[Admin, Editor]` a `[Admin, Soporte]`; `PlanUsageWidget::canView()` nuevo (`[Admin, Soporte]`); `UserResource.php` con las 2 opciones/colores nuevos. Ver ADR-078 (2do addendum)/PROGRESS.md/CURRENT_STATE.md. **Pendiente: `vendor/bin/pint --dirty --format agent`, `php artisan test --compact`, y confirmación en vivo del Tech Lead con cuentas reales de los 5 roles — el punto más sensible es que `Editor` ya no vea Multimedia/Menús/Sliders/Formularios pero SÍ siga viendo Contactos. Commit pendiente.** Owner: libre.)
> Antes, mismo día (**ADR-078: autorización real por rol en Studio (Admin/Editor/Author) — jerarquía de Policies + fix de `setPermissionsTeamId()` global** — bug de seguridad real reportado en vivo por el Tech Lead con capturas: un colaborador creado con rol Editor tenía acceso total, idéntico al Admin. Causa doble: `spatie/laravel-permission` asignaba roles bien pero NINGÚN Resource/Page de Filament los consultaba (solo `ContactPolicy` existía, y solo chequeaba tenant); `setPermissionsTeamId()` solo se llamaba en 3 puntos aislados de `UserResource`, nunca globalmente (fix en `SyncTenantManagerWithFilament`, sin el cual cualquier Policy nueva basada en rol habría fallado en silencio). Matriz de acceso acordada vía `AskUserQuestion` (Admin=control total, Editor=casi todo salvo Usuarios, Author=solo Page/Post/Service/Testimonial sin borrar ni publicar). Jerarquía de Policies pedida explícitamente por el Tech Lead para no repetir código: `TenantRolePolicy` (base) → `EditorManagedPolicy` → concretas por Resource (10 Resources cubiertos, la mayoría clases vacías). `canAccess()` explícito (trait `RestrictsPageToRoles`) en las 4 Pages sin modelo Eloquent (`ApiTokens`/`ApiPlayground`/`ApiDocumentation`/`Preferences`) — confirmado que Filament NO delega a ninguna Policy en Pages custom, a diferencia de Resources. Bloqueo de publicar/activar para `Author` (trait `LocksPublishingForAuthor`) en `status`/`published_at` (Page/Post/Service) e `is_visible` (Testimonial, las 3 superficies: form/columna inline/bulk actions). `filament-shield` explícitamente NO instalado (el Tech Lead mostró interés pero lo pospuso — quedaría como función de PAGO, oculta para Free/Auspicio); la idea de Auspicio con roles preset + custom queda solo como nota de futuro en el ADR, sin diseño. Ver ADR-078/PROGRESS.md/CURRENT_STATE.md. **Pendiente: `vendor/bin/pint --dirty --format agent`, `php artisan test --compact` (sin tests nuevos todavía), y sobre todo probar en vivo en Studio con una cuenta `Editor` y una `Author` reales — el reporte original del Tech Lead fue justamente que el comportamiento en vivo no coincidía con el rol elegido. Commit pendiente.** Owner: libre.)
> Antes, mismo día (**Stage: habilitar R2 con bucket propio — documentado, pendiente de ejecución fuera de este sandbox** — el Tech Lead pidió activar R2 en Stage "por seguridad y pruebas fidedignas" (hoy corre en disco local, Producción ya en R2). Sin cambios de código: `config/filesystems.php` ya resuelve local/R2 por `.env` (ADR-077). Decisión tomada con el Tech Lead: bucket+token de Cloudflare PROPIOS para Stage (no compartir con Producción). Documentado como addendum de ADR-004 (`docs/context/DECISIONS.md`) con el procedimiento completo. **Pendiente operativo del Tech Lead (requiere Cloudflare dashboard + SSH al servidor, no disponibles en este sandbox):** 1) crear bucket `stamless-storage-stage` (o el nombre que prefiera) + token de API acotado a ese bucket en Cloudflare; 2) editar `/var/www/vhosts/stage_stamless/.env`: `FILESYSTEM_DISK=r2` + `R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET`/`R2_ENDPOINT`/`R2_URL`; 3) `php artisan config:clear && php artisan config:cache`; 4) `php artisan media:sync-r2 --dry-run` y luego real; 5) confirmar visualmente en Studio de Stage que los thumbnails siguen cargando. De paso corregido `.env.example`: bucket placeholder `genesis-media` (codename viejo) → `stamless-storage` (nombre real de Producción). Ver PROGRESS.md/CURRENT_STATE.md/DECISIONS.md. Owner: libre.)
> Antes, mismo día (**Tabla de límites de tokens en `docs/api/v1.md` (Free=5, Auspicio/Convenio=10) → ejemplo genérico** — el Tech Lead preguntó si era fija o dinámica; confirmado que es Markdown estático sin ninguna lectura en runtime de `Tenant::maxApiTokens()` — reemplazada por explicación genérica + referencia a Console para el límite real vigente, evita que la doc quede desactualizada si el límite cambia en el código. Ver PROGRESS.md/CURRENT_STATE.md. Owner: libre.)
> Antes, mismo día (**`docs/api/v1.md`/`openapi.v1.yaml`: sacadas todas las anotaciones tipo "agregado en 2026-09-XX"/"excepción de tal fecha"** — pedido del Tech Lead: "no es necesario poner fechas por mejoras o cambios" — un guideline de referencia no debe leerse como changelog. Sacadas 8 anotaciones de fecha en `v1.md` + 2 en `openapi.v1.yaml`, conservando la info técnica real en cada caso; de paso limpiados 2 restos de framing histórico ("Hasta ahora...") y una referencia interna colada (`ver ADR-045`). Ver PROGRESS.md/CURRENT_STATE.md. **Pendiente: commit + sumar al deploy pendiente.** Owner: libre.)
> Antes, mismo día (**Genericización de ejemplos de contenido en la doc pública del API (`v1.md`/`openapi.v1.yaml`) — sin rubro seguros/CICA360 — + fix de URL de media ficticia + voseo residual en `openapi.v1.yaml`** — pedido del Tech Lead viendo un ejemplo real de CICA360 (quote de `GET /testimonials`): "en el demo no puede haber nada de otro tenant como ejemplo". Reescritos a placeholders neutros todos los valores de ejemplo en `docs/api/v1.md` (pages/posts/services/testimonials/menus/sliders/media/forms), el preset `posts.show` de `ApiPlayground.php` (slug real hardcodeado) y el `tenant_slug` default de `openapi.v1.yaml`. Bonus encontrado: 4 respuestas de error de `openapi.v1.yaml` seguían con voseo desactualizado, nunca tocadas por el barrido de voseo de una sesión anterior — reconciliadas con `bootstrap/app.php`. Corrección de seguimiento: el ejemplo de `GET /media/{uuid}` no era solo poco genérico sino FICTICIO (`cdn.stamless.com/{tenant_slug}/...` no existe) — investigado el código real (`Media::url()`, R2) y corregido a la estructura real (`R2_URL`/`pub-<hash>.r2.dev`, slug como prefijo de filename, no carpeta). Ver PROGRESS.md/CURRENT_STATE.md. **Pendiente: commit + sumar al deploy pendiente.** Owner: libre.)
> Antes, mismo día (**Fix real: API Documentation/API Playground ahora personalizan el tenant slug de ejemplo (antes `cica360` fijo)** — `ApiDocumentation::readMarkdown()` sustituye `cica360` (minúscula, solo segmentos de URL) por el slug del tenant actual de Console; `ApiPlayground::mount()` precarga `tenant_slug` real en vez del literal. Ver PROGRESS.md/CURRENT_STATE.md. **Pendiente: confirmar en vivo con un tenant distinto de CICA360.** Owner: libre.)
> Antes, mismo día (**Rebrand documentación pública del API a "Stamless CMS Headless" + refuerzo de aislamiento por tenant** — `docs/api/v1.md`/`openapi.v1.yaml` dejan de citar el codename "Genesis CMS", y las secciones de tokens/SMTP propio/deploy automático dejan explícito que son configuración exclusiva por tenant. Ver PROGRESS.md/CURRENT_STATE.md. Owner: libre.)
> Antes, mismo día (**Fix real: `deploy.sh` excluía `/docs/` completo del rsync — "API Documentation" rota en cualquier ambiente desplegado, sin relación con el contenido** — el archivo `docs/api/v1.md` SIEMPRE estuvo correcto en el repo; `deploy.sh` nunca lo llevaba al servidor porque excluía toda la carpeta `/docs/` (pensado solo para `docs/context/`, documentación interna). Fix: exclusión acotada a `docs/context/`. Ver PROGRESS.md/CURRENT_STATE.md. **Pendiente urgente: correr `./deploy.sh` + `./production.sh` para que "API Documentation" y `openapi.v1.yaml` carguen en vivo — sin ese deploy, el error sigue visible pese a que el repo ya está bien.** Owner: libre.)
> Antes, mismo día (**API Playground + API Documentation actualizados, barrido de voseo rioplatense (#177)** — `ApiPlayground.php` verificado: los 11 endpoints reales ya estaban en `PRESETS`, solo se corrigió tono en 4 strings. `docs/api/v1.md` gana `GET /forms/{slug}`, `POST /forms/{slug}/submit` reescrito (honeypot, `X-Forwarded-For`/`X-Origin-Country`), `GET /settings/tracking`, sección de límites/origen de tokens, patrón de proxy server-side para no exponer el token, y 2 secciones nuevas de funciones de Console (SMTP propio, Despliegue automático Git — no son endpoints, documentadas por pedido explícito). `bootstrap/app.php`: los 7 mensajes fijos del envelope de error reescritos a español neutro (**texto real de producción**, `errors.code` sin cambios). Voseo corregido también en `FormSubmissionController.php`, `UserResource.php`, `MediaResource.php`, `ApiTokens.php`, `PageResource.php` — alcance ampliado más allá de los 2 archivos nombrados, por "estas usando mucho" leído como problema sistémico. Ver PROGRESS.md/CURRENT_STATE.md. **Pendiente: correr `pint` en un entorno con PHP, evaluar ADR para el cambio de `bootstrap/app.php`, commit.** Owner: libre.)
> Antes, mismo día (**✅ Cerrado: bug de thumbnails rotos en R2, confirmado resuelto en producción** — cadena de 4 fixes del mismo día (estado del `ImageColumn` → chequeo de existencia contra R2 → visibilidad/URL firmada → disco `'public'` canónico en config) desplegados y confirmados por el Tech Lead: Servicios/Testimonios/Publicaciones/Biblioteca de Medios muestran sus thumbnails bien. Los 39 archivos que quedan en local de producción son backup intencional del Tech Lead, no hace falta `media:sync-r2`. Nada pendiente de este hilo salvo lo ya operativo de rutina (`php artisan migrate` si no se corrió, `pint`, tests). Owner: libre.)
> Antes, mismo día (**Fix de fondo (4to, mismo hilo): disco `'public'` canónico en `config/filesystems.php`, reemplaza los `->visibility('public')` por columna** — pedido del Tech Lead: corregir en la raíz en vez de parchar cada `ImageColumn`. `'public'` ahora resuelve local o R2 según `FILESYSTEM_DISK`; nuevo disco fijo `'local_public'` para código que necesita el filesystem local garantizado. `MediaUpload`/`MediaResource` simplificados al literal `'public'`. Migración normaliza `Media.disk` ya guardado (`'r2'`→`'public'`). **Regresión corregida en el mismo turno:** `SyncMediaToR2Command` (`media:sync-r2`, probablemente el comando de sincronización que preguntaste al inicio de este hilo) y `Cliente0MediaSeeder` asumían `'public'`=local siempre — reescritos + test actualizado. Ver PROGRESS.md/CURRENT_STATE.md. **Pendiente: `php artisan migrate`, `php artisan test --compact --filter=SyncMediaToR2CommandTest`, deploy (`deploy.sh` + `production.sh`) y confirmación visual final en Servicios/Testimonios/Publicaciones/Biblioteca de Medios.** Owner: libre.)
> Antes, mismo día (**3er fix real, mismo hilo: `visibility('public')` en los 4 `ImageColumn` — evitar URL firmada de R2** — el Tech Lead confirmó por tinker (`Storage::disk('r2')->url()`) que disco/credenciales están bien (URL pública simple correcta), pero el `<img>` roto, tras el 2do fix, mostraba una URL FIRMADA de R2 (`*.r2.cloudflarestorage.com/...?X-Amz-Signature=...`). Causa: `ImageColumn::getVisibility()` infiere `'private'` cuando el nombre del disco no es literalmente `'public'` (acá `'r2'`), sin mirar la config real del disco — dispara `temporaryUrl()` en vez de `url()`. Fix: `->visibility('public')` explícito en `ServiceResource`, `TestimonialResource`, `PostResource`, `MediaResource`. Ver PROGRESS.md/CURRENT_STATE.md. **Pendiente: deploy (`deploy.sh` + `production.sh`) y confirmación visual — con los 3 fixes acumulados del día no queda ninguna rama conocida de `ImageColumn` sin cubrir; si sigue roto, revisar si `media.stamless.com` carga directo en el navegador.** Owner: libre.)
> Antes, mismo día (**2do fix real, mismo hilo: `checkFileExistence(false)` en los 4 `ImageColumn` sobre `Media`** — el Tech Lead confirmó, tras correr `deploy.sh` + `production.sh` completos (con cache-clear incluido) sobre el fix anterior del estado, que el thumbnail seguía roto — descartando timing/cacheo de Laravel. Causa real: `ImageColumn::getImageUrl()` de Filament hace un `$storage->exists($state)` (HeadObject) contra R2 antes de construir la URL, y si falla devuelve `null` — mismo síntoma (`src=""`), causa distinta al bug del estado ya corregido. `MediaUpload::previewUrl()` (formulario, siempre funcionó) nunca hizo ese chequeo. Fix: `->checkFileExistence(false)` en `ServiceResource`, `TestimonialResource`, `PostResource` y `MediaResource` (preventivo). Ver PROGRESS.md/CURRENT_STATE.md. **Pendiente: deploy (`deploy.sh` + `production.sh`) y confirmación visual del Tech Lead — este cambio no depende de ningún cache de Laravel, así que si sigue roto después el problema sería otro (credenciales/permisos reales del token R2 en producción).** Owner: libre.)
> Antes, mismo día (**Fix producción: thumbnails con `src` vacío en listados de Servicios/Testimonios/Publicaciones** — el Tech Lead reportó, con capturas de producción, íconos de imagen rotos en los listados de Servicios y Testimonios/Casos de Éxito (DevTools confirmó `<img>` sin `src`, no una URL rota), mientras el mismo registro mostraba su imagen real en el formulario de edición. Causa: `ImageColumn::make('relacion.path')` (notación de punto) resolvía bien el disco vía `disk()` (accede a `$record` completo) pero NO el estado/`src` de la columna por esa misma notación — mismo patrón en `ServiceResource` (`image.path`), `TestimonialResource` (`avatar.path`) y `PostResource` (`featuredImage.path`, el Tech Lead confirmó que Blog/Publicaciones debía revisarse también). Fix: `->getStateUsing(fn ($record) => $record?->relacion?->path)` explícito en las 3 columnas. Grep confirmó que no queda ningún otro caso sin cubrir en `app/Filament`. No requiere migración — cambio de código puro. Ver PROGRESS.md/CURRENT_STATE.md. **Pendiente: confirmar en vivo en producción que los 3 listados vuelven a mostrar el thumbnail correcto (deploy/pull del código nuevo).** Owner: libre.)
> Antes, mismo día (**Deploy webhook: tenant self-service de repo/token de Git + checkbox "Automatización activa" (3ra actualización de ADR-072)** — pedido del Tech Lead: "no debería poder el cliente, para su tenant, vincular a git... si no, no habilita el check de automatización" — nueva sección "Despliegue automático (Git)" en `Preferences.php`, el tenant carga `deploy_repo`/`deploy_token` (antes exclusivo del operador vía tinker); nueva columna `deploy_enabled` + `Tenant::hasAutoDeployActive()` (gate real que ahora usan `DeployTriggerObserver`/`TriggerFrontendDeploy`) — el checkbox no se puede tildar sin credenciales completas. Corrección inmediata del Tech Lead sobre el 1er intento: ejemplo de repo genérico (no un cliente real) + 3 frases nuevas + 2 preexistentes reescritas de voseo rioplatense a español neutro en el mismo archivo (ADR-051). 2 tests nuevos en `FrontendDeployWebhookTest`. Ver 3ra actualización de ADR-072/PROGRESS.md/CURRENT_STATE.md. **Pendiente: `php artisan migrate` (suma la migración `add_deploy_enabled_to_tenants_table` a la lista ya pendiente del día — SMTP propio, form_id→form_slug, reindexado de options), y que el Tech Lead tilde manualmente "Automatización activa" en Preferencias para CICA360 (el checkbox arranca en `false` por default aunque ya tuviera repo/token seteados por tinker).** Owner: libre.)
> Antes, mismo día (**Fix real: `FormField.options`/`validation_rules` se guardaban como objeto JSON en vez de array (mismo bug de ADR-071, nunca portado a `FormResource`)** — el Tech Lead reportó que el campo "País" del form "Contacto" desapareció del sitio público de cica360 pese a estar activo, tras un `TypeError (field.options ?? []).map is not a function` en producción. Causa real: `saveRelationshipsUsing()` de "fields" en `FormResource.php` no reindexaba los Repeaters anidados (`options`/`validation_rules`) antes de guardar, quedaban keyeados por UUID de Livewire y se serializaban a JSON como objeto en vez de array — mismo bug ya resuelto para `colophon` en ADR-071, nunca portado acá. Fix: `FormResource::reindexRepeaterState()` nuevo + migración de datos (`2026_09_18_060000_reindex_form_field_options_and_validation_rules`) que corrige los `FormField` ya guardados con el bug. Ver addendum ADR-073/PROGRESS.md/CURRENT_STATE.md. **Pendiente: `php artisan migrate` (corre la migración de reindexado — sin ella el campo "País" de CICA360 sigue oculto hasta que se reabra y regrabe a mano en Studio), más las demás migraciones ya pendientes del día (SMTP propio, form_id→form_slug).** Owner: libre.)
> Antes, mismo día (**Forms Fase 2 implementada: wiring dinámico del front cica360 (addendum ADR-073)** — reporte en vivo del Tech Lead sobre "Contacto" en CICA360: desactivar campos en Studio no tenía efecto en el sitio público. Fix de contrato previo: `PageResource.php`/`Cliente0ContentSeeder` pasan de guardar `content.form_id` (numérico) a `content.form_slug` (el API público solo resuelve por slug), con migración de datos para bloques ya guardados. `ContactFormBlock.astro` (cica360) ahora resuelve `getForm(content.form_slug)` y pasa el `Form` a `<ContactForm />`; `ContactForm.tsx` reescrito por completo — de 7 campos hardcodeados a render dinámico de `form.fields` por `FormFieldTypeEnum`, con validación de formato leyendo `FormFieldData.validation_rules` en vez de reglas por nombre. `npx tsc --noEmit --ignoreDeprecations 6.0` corrió limpio (exit 0) sobre todo el proyecto cica360. Ver addendum ADR-073/PROGRESS.md/CURRENT_STATE.md (ambos repos). **Pendiente: `php artisan migrate` (suma la migración de datos nueva a la lista ya pendiente de ADR-076), y que el Tech Lead confirme en vivo — desactivar un campo del form "Contacto" en Studio y ver que desaparece del sitio publicado tras el próximo rebuild/deploy.** Owner: libre.)
> Antes, mismo día (**SMTP propio por tenant, opcional, híbrido con fallback (ADR-076)** — el Tech Lead preguntó si convenía SMTP único compartido o propio por tenant; se implementó el enfoque híbrido recomendado: nueva sección "SMTP propio (opcional)" en `Preferences.php` (host/puerto/usuario/contraseña cifrada/cifrado/from), sin configurar sigue usando el SMTP global de la plataforma. La parte técnica no obvia: nuevo trait `App\Mail\Concerns\UsesTenantSmtp` (usado por `ContactFormSubmitted`/`ContactFormReceipt`) que sobreescribe `Mailable::prepareMailableForDelivery()` — el único hook que corre en cada intento real de envío, síncrono o desde el worker de cola, leyendo la config del tenant fresca (las relaciones Eloquent no sobreviven la serialización de la cola). Botón "Probar conexión" manda un email real con los datos tipeados en el momento. Ver ADR-076/PROGRESS.md/CURRENT_STATE.md. **Pendiente: `php artisan migrate`, `php artisan test --compact`, `vendor/bin/pint --dirty --format agent`, y que el Tech Lead pruebe el botón "Probar conexión" con un SMTP real (Gmail App Password o Mailtrap con credenciales completas) — sin runtime de PHP en este sandbox, ninguno se corrió acá.** Owner: libre.)
> Antes, mismo día (**Fix producción: `BindingResolutionException` al editar cualquier Form en Studio (addendum ADR-075)** — closure crudo de validación custom en `notification_email` (`->rules([function (string $attribute, ...) {...}])`) rompía Filament al intentar EVALUARLO como uno de sus propios closures inyectables (`EvaluatesClosures`), sin poder resolver `$attribute`; el error ocurría al montar el formulario, rompiendo la edición de CUALQUIER Form existente en producción, reportado por el Tech Lead con captura de un 500 real en `studio.stamless.host`. Fix: envolver en closure exterior sin parámetros que devuelve el closure de validación real — `->rules([fn (): \Closure => function (string $attribute, mixed $value, \Closure $fail) {...}])`, patrón documentado por Filament. Archivo: `FormResource.php`. Balance verificado manualmente (38/38, 468/468, 57/57). Ver addendum ADR-075. **Pendiente: confirmar en Studio que editar un Form ya no rompe y que la validación multi-email sigue funcionando.** Owner: libre.)
> Antes, mismo día (**Emails de notificación de formularios: marca del tenant, cola, multi-email y "copia al usuario" real (ADR-075)** — plantilla HTML propia compartida por todos los tenants (`emails/layouts/branded.blade.php`, reemplaza el markdown genérico de Laravel), con logo del tenant inyectado dinámicamente (`Setting` `branding.logo_id`, nueva sección "Marca" en `Preferences.php`). `Form::notification_email` admite varios correos separados por coma (to + cc); `notification_intro` nuevo para personalizar la intro del email admin. "Enviar copia a quien completa el formulario" (`send_copy_to_submitter`) implementado por primera vez — existía como columna/toggle muerto desde el MVP; nuevo Mailable `ContactFormReceipt` reusa la "Página de Agradecimiento" (`ContactSubmissionService::resolveThankYouData()`, movido desde el controller). Ambos Mailables ganan `ShouldQueue` (envío por cola, pedido explícito). 3 rondas de ajustes de layout en `FormResource.php` (Notificaciones a 1 columna, Seguridad al final y ancho completo) + corrección de tono (voseo → español neutro, alineado con ADR-051). Ver ADR-075/PROGRESS.md/CURRENT_STATE.md. **Pendiente: `php artisan migrate`, `php artisan test --compact`, `vendor/bin/pint --dirty --format agent`, validación visual completa en Studio y confirmar `queue:work` corriendo — sin runtime de PHP en este sandbox, ninguno se corrió acá.** Owner: libre.)
> Antes, mismo día (**Formularios por tenant, Fase 1: `FormResource` en Studio con Builder + block-picker, Página de Agradecimiento por Form (ADR-073/ADR-074)** — gap real: `Form`/`FormField` eran modelos multi-tenant completos desde el MVP, pero sin ninguna pantalla en Studio para gestionarlos. Nuevo `FormResource` (grupo "Clientes"): CRUD de `Form` + builder de campos vía `Builder` con block-picker con ícono (mismo patrón que "Agregar bloque" en Contenidos), cada campo eligiendo una `FormFieldDefinition` del catálogo (type/validation_rules FIJOS e internos vía `Hidden`) o "Campo personalizado" (100% editable). `Tenant::maxForms()` nuevo. `FormFieldTypeEnum::TelCountry` generaliza el WhatsApp+país+IP de CICA360 a tipo de catálogo reutilizable. La "Página de Agradecimiento" se trasladó de `Setting` tenant-wide a columnas `Form::thank_you_*`. Ver ADR-073/ADR-074/PROGRESS.md/CURRENT_STATE.md. Fase 2 (wiring del front cica360) y Fase 3 (adopción real del tipo `TelCountry` en el "WhatsApp" ya sembrado, requiere re-correr seeders) quedan pendientes, no iniciadas. Owner: libre.)
> Antes, 2026-09-17 (**Fase 6 (post-MVP) adelantada: deploy webhook por tenant (ADR-072)** — mecanismo opt-in por tenant (`Tenant::deploy_repo`/`deploy_token`) que dispara automáticamente el rebuild+deploy del front headless al guardar contenido (`DeployTriggerObserver` en 9 modelos → `TriggerFrontendDeploy`, Job con debounce vía `ShouldBeUnique` → `FrontendDeployService`, `POST /repos/{repo}/dispatches` a GitHub). Ver ADR-072/PROGRESS.md/CURRENT_STATE.md. **3 pendientes operativos del Tech Lead antes de que funcione en producción: (1) setear `deploy_repo`/`deploy_token` del tenant CICA360 vía tinker, (2) secrets `FTP_SERVER`/`FTP_USERNAME`/`FTP_PASSWORD` en GitHub (repo cica360), (3) confirmar worker de colas corriendo (`queue:work`).** Además: `php artisan migrate`, `vendor/bin/pint --dirty --format agent`, `php artisan test --compact --filter=FrontendDeployWebhookTest` — sin runtime de PHP en este sandbox, ninguno de los 3 se corrió acá. Owner: libre.)
> Antes, mismo día (**Remoción de `properties.animation` de Filament (ADR-070) + sistema sitewide de reveal-on-scroll estático en cica360** — pedido del Tech Lead pivotó de "usar el selector de animación existente" a "quitarlo de Filament, que la animación sea estática por frontend/cliente". `Select::make('animation', ...)` eliminado de `PropertiesSchema.php` + retirado de los 8 sitios de `PageResource.php`/1 de `ServiceResource.php`. Del lado cica360: motor `data-reveal`/`IntersectionObserver` construido y cableado en los 18 componentes de `blocks/` + `servicios/[slug].astro` + `blog/*`. Ver DECISIONS.md (ADR-070) y PROGRESS.md/CURRENT_STATE.md de ambos repos. **Constraint activo del Tech Lead: no se hizo git commit/push ni deploy en ningún repo — solo para probar en local.** Pendiente: validación visual del Tech Lead. Owner: libre.)
> Antes, 2026-09-15 (**Spatie Roles & Permissions multi-tenant, Super Admin, Social Login, Auth Designer y Contadores de Caracteres en tiempo real** — Spatie Roles & Permissions con `teams => true` y `team_foreign_key => tenant_id`; Super Admin global con bypass `Gate::before`; gestión de usuarios en Studio (`UserResource`) acotada a `tenant_id` con badges y límites por plan (`Tenant::maxUsers`); Social Login con `matondojk/filament-social-login`; Login estilizado con `caresome/filament-auth-designer`; contadores de caracteres interactivos en tiempo real con `schmeits/filament-character-counter` en SEO, Colophon, Extracto de Blog, Intro de Servicios y Testimonios. Suite de tests: **103 tests pasando al 100%**. Owner: libre.)
> Antes, mismo día (**Blindaje integral de Scope Tenant: aislamiento en Studio y API v1, soft-deletes slug uniqueness con sufijo en restore e is_home por tenant** — blindaje multi-tenant exhaustivo en Studio y API REST v1. Slugs únicos solo para registros activos (`deleted_at IS NULL`), restauración con sufijos incrementales (`-restaurado`, `-restaurado-2`, etc.), desactivación de `is_home` previa acotada al tenant sin tocar otros tenants, duplicación/replicación de slugs e items hijos con `tenant_id` estricto, autorización en `ApiTokens` y defensa en profundidad en API controllers. Suite completa: **97 tests pasando al 100%**. Owner: libre.)
> Antes, mismo día (**Studio (UX): opciones agrupadas por tipo en "Copiar a otro contenido", label "Contenido destino" y blindaje de scope tenant** — en el modal de acción "Copiar a otro contenido" en `PageResource`, el selector `target_page_id` usa label `Contenido destino`, helper `Sólo se listan contenidos que aceptan este tipo de bloque.` y agrupa las opciones bajo encabezados por tipo con scope estricto por `tenant_id` en consulta y ejecución. Tests pasando 90/90. Owner: libre.)
> Antes, mismo día (**Studio (UX): fix de z-index y footer sticky en modales — el dropdown del selector ya no queda detrás del footer** — en la acción "Copiar bloque a otra página" en Contenidos, el footer del modal con "Copiar"/"Cancelar" tapaba el menú desplegable del Select de página destino. Se acotó el footer sticky (`position: sticky`, `z-index: 50`) exclusivamente a `.fi-modal-slide-over`, y se elevó `.fi-dropdown-panel` a `z-index: 100 !important` en `api-console.css` y `theme.css`. Tests pasando 86/86. Owner: libre.)
> Antes, mismo día (**Selector de footer dinámico en Servicios (`properties.footer_page_id`) + resolución en API y frontend** — selector en Studio bajo Configuración, resolución con bloques resueltos en `GET /services/{slug}`, sembrado de `footer-principal` en `Cliente0ServicesSeeder` y renderizado mediante `BlockRenderer` en `servicios/[slug].astro`. Tests pasando 86/86, Pint limpio, TypeScript limpio. Owner: libre.)
> Antes, 2026-09-13 (**"Copiar a otra página" por bloque en el Builder de Contenidos** — `extraItemActions()` junto a "Duplicar", copia inmediata vía `Block::create()` a la página destino elegida en un modal, filtrada por compatibilidad bloque↔tipo de página (`PageResource::isBlockAllowedForPageType()`, nueva) — ver PROGRESS.md. Antes, mismo día: 3 widgets nuevos del Escritorio (uso de plan, KPIs de leads, últimos contactos) + Dashboard Nivel 1 (widget de bienvenida con badge de plan, se saca `FilamentInfoWidget`) + Nivel 2 (`ContactResource` de Filament habilitado — ver ADR nuevo en DECISIONS.md) + badges de uso en sidebar/tabs de Contenidos + límite de Menús + límites de Contenidos por tipo (Auspicio 10/5/5, Free 5/3/3). Pendiente: confirmación visual del Tech Lead de todo lo anterior, `npm run build` para el badge del widget si aplica. Owner: libre.)
> Antes, 2026-09-11 (**Formulario "Contactame": 3 campos nuevos (`city`/`country`/`area_of_interest`) sembrados en el form `contacto` + `ContactForm.tsx` (cica360) rediseñado completo contra el mockup real** — ver PROGRESS.md/CURRENT_STATE.md (ambos repos). Pendiente: `db:seed` + `pint` + confirmación visual del Tech Lead. Owner: libre.)
> Antes, mismo día (**Bloque `testimonials_grid` nuevo — catálogo completo de "Casos de éxito", mismo tratamiento que `services_grid`** — ver ADR-050 en DECISIONS.md, PROGRESS.md/CURRENT_STATE.md. Pendiente: `db:seed` + `pint` + confirmación visual del Tech Lead contra el mockup. Owner: libre.)
> Antes, 2026-09-10/11 (**`services_grid` resuelto en runtime + catálogo real de 9 servicios + varios ajustes visuales de `ServicesGrid.astro`** — ver ADR-049, PROGRESS.md/CURRENT_STATE.md para el detalle completo de esta racha. Owner: libre.)
> Antes, 2026-09-07 (**"Sobre CICA": agregados bloques `testimonials`/`logos` clonados de la home + fix `background_color` faltante en `features`** — ver PROGRESS.md/CURRENT_STATE.md. Pendiente: `db:seed` + confirmación visual del Tech Lead sobre el orden final de secciones. Owner: libre.)
> Antes, mismo día (**Corrección: `features.content_format` (por item) → `properties.list_style` (por bloque) + renombre de enum** — amplía ADR-047. `FeatureContentFormatEnum` → `FeatureListStyleEnum`; selector movido a `properties`. Ver PROGRESS.md/CURRENT_STATE.md. Pendiente: confirmar con el Tech Lead si quiere refactorizar a enum los 8 campos hardcodeados preexistentes de `PropertiesSchema.php` (`background_type`, `content_width`, `padding_y`, `text_align`, `link_radius`, `link_size`, `gradient_direction`, `media_radius`) — deuda no introducida en este cambio. Owner: libre.)
> Antes, mismo día (**Rediseño completo del bloque `features`** — ver ADR-047 en DECISIONS.md y PROGRESS.md/CURRENT_STATE.md. Pendiente de auditoría (no corregido en este cambio): `cta`/`colophon` tienen un `background_image_id` sin prefijo `content.` que nunca se guarda (mass-assignment lo descarta en silencio) — ver ADR-047. Owner: libre.)
> Antes, 2026-09-06 (**"Sobre CICA": 2do bloque reemplazado por `rich_text` simple sin heading** — ver PROGRESS.md/CURRENT_STATE.md. Owner: libre.)
> Antes, mismo día (**Título de Servicios y Casos de Éxito revertido al corto del diseño** — "Servicios", "Casos de éxito"; "Contactame" queda como única excepción con tono argento; subtítulos sin cambios. Ver PROGRESS.md/CURRENT_STATE.md. Owner: libre.)
> Antes, mismo día (**Título/subtítulo de los 3 bloques `heading` nuevos con voseo rioplatense** — "Contactame", "Descubrí nuestros servicios", "Conocé nuestros casos de éxito". Ver PROGRESS.md/CURRENT_STATE.md. Owner: libre.)
> Antes, mismo día (**Bloque `heading` replicado a Contacto, Servicios y Casos de Éxito** — ver PROGRESS.md/CURRENT_STATE.md. Owner: libre.)
> Antes, mismo día (**Bloque `heading` de "Sobre CICA": `overlay_opacity` default sembrado en 90 (no 100)** — ver PROGRESS.md/CURRENT_STATE.md. Owner: libre.)
> Antes, mismo día (**Bloque `heading` de "Sobre CICA": sembrado `overlay_color`/`overlay_opacity` (faltaban por completo)** — ver PROGRESS.md/CURRENT_STATE.md. Owner: libre.)
> Antes, 2026-09-05 (**Heading de "Sobre CICA" sembrado + fix real de mismatch `'waves'`/`'wave'`** — ver PROGRESS.md/CURRENT_STATE.md. Owner: libre.)
> Antes, mismo día (**Fix real: picker de "Añadir bloque" perdía las primeras opciones al flipear hacia arriba** — override de `resources/views/vendor/filament-forms/components/builder/block-picker.blade.php` agregando `:max-height="'400px'"` al dropdown compartido de Filament, activando su `fi-scrollable` nativo. Ver PROGRESS.md/CURRENT_STATE.md para el detalle completo de la investigación y por qué se descartó duplicar el botón. Pendiente confirmación visual del Tech Lead — sin runtime PHP en este sandbox. Owner: libre.)
> Antes, 2026-09-02 (**Mejoras de UX/UI en el módulo de Contenidos**: Títulos dinámicos de SlideOver según tipo de contenido (*Crear Página*, *Crear Aviso Legal*, *Editar Cabecera (Header)*, etc.), tabs de filtrado (*Páginas*, *Legales*, *Secciones*), botones dinámicos de estado vacío per-tab, footer sticky de SlideOver con `z-index: 50 !important` para no solaparse con sliders/builders, y ocultamiento total de `url_preview` en partials (headers/footers). Tests pasando 100% (43/43). Owner: libre.)  
> Owner actual: _(libre)_  
> Estado: **Ready — API cerrada de producto. Próxima sesión = frontend CICA360 en shared hosting.**

---

## Tarea activa

**Frontend CICA360 (shared hosting) consumiendo REST v1**

Lado producto (API) **cerrado**: REST v1, auth Sanctum, envelope, seeds CICA360, playground en Console, hosts Genesisly.

Fase inmediata de ingeniería:

> **Sitio estático de CICA360 en el shared hosting del cliente, consumiendo solo estos endpoints con Bearer `content:read` / `forms:submit`.**

```
GET  /v1/cica360/pages
GET  /v1/cica360/pages/{slug}
GET  /v1/cica360/menus/menu-principal
GET  /v1/cica360/sliders/home
POST /v1/cica360/forms/contacto/submit
```

Host API: `config('genesis.urls.api')` (sin prefijo `/api`). GraphQL y marca fina **fuera** hasta que el sitio esté en el aire.

---

## Objetivo del MVP (Cliente 0)

Entregar una plataforma CMS usable en modo **Headless** para una consultora uruguaya de seguros y temas jurídicos, de modo que:

1. Un **tenant** represente al Cliente 0.
2. El equipo admin gestione **páginas, bloques y media** desde Filament.
3. Un **frontend estático** (Astro o Next.js export) consuma la **API REST** y se aloje en shared hosting del cliente.
4. El tenant opere bajo plan **Free Forever** (sin cobro, con límites de core).

---

## Criterios de aceptación del MVP

### Backend / plataforma

- [x] Proyecto Laravel 13 instalado y documentado en `README.md`
- [x] Filament panel accesible (login admin)
- [x] PostgreSQL configurado; migraciones base corriendo
- [x] Modelo multi-tenant: `tenants`, `domains`, `tenant_id` en entidades de negocio
- [x] Aislamiento: un tenant no puede leer/escribir datos de otro (test mínimo)
- [x] CRUD de **Pages** con **Blocks** tipados (al menos: hero, rich_text, image, cta)
- [x] Media upload hacia disk configurable (local en dev; R2 en prod)
- [x] API REST pública versionada (`/api/v1/...`) para pages por slug, menús y settings públicas — ver ADR-016
- [x] CORS y lectura pública de contenido publicado
- [x] Seed o setup del tenant Cliente 0 + usuario admin (tenant CICA360 + owner, ver ADR-014)

### Frontend Cliente 0

- [ ] Decisión documentada: Astro **o** Next.js (static export)
- [ ] Sitio estático que consume la API (home + al menos 2 páginas internas)
- [ ] Build exportable a HTML/CSS/JS para shared hosting
- [ ] Variables de entorno para `API_BASE_URL` / tenant domain

### Producto / ops

- [x] Plan Free Forever reconocido en datos (`PlanSeeder`: límites simples — max_users/max_pages/max_posts/max_storage_mb)
- [x] Docs de contexto actualizados (`CURRENT_STATE`, `PROGRESS`, ADRs si hubo cambios) — cierre API 2026-08-20
- [ ] Instrucciones de setup local en `README.md`

### Fuera de alcance ahora (hasta que el sitio CICA360 esté en el aire)

- GraphQL
- Marca fina / polish visual de Genesisly
- `FriendlyDate` en resto de Console, honeypot/reCAPTCHA
- Módulos verticales completos (insurance/legal como producto)
- Modo monólito Blade
- Billing / pasarelas de pago
- Marketplace de bloques
- Multi-idioma avanzado (salvo necesidad explícita del cliente)

---

## Desglose de subtareas prioritarias

### P0 — Fundación (hacer primero)

| # | Subtarea | Estado |
|---|----------|--------|
| 1 | Scaffold Laravel 11/12 + Git baseline | Done |
| 2 | Configurar PostgreSQL + `.env.example` | Done |
| 3 | Instalar Filament y panel admin | Done |
| 4 | Migraciones `tenants`, `domains`, users tenant-aware | Done |
| 5 | Middleware / resolver de tenant + global scopes | Done |

### P1 — Contenido y API

| # | Subtarea | Estado |
|---|----------|--------|
| 6 | Modelos Page + Block (migraciones + Eloquent) | Done |
| 6b | Modelos Post, Slider/Slide, Menu/MenuItem (migraciones + Eloquent) | Done |
| 7 | Media library (migración + modelo `Media`) + disk R2/local | Done |
| 7b | Plans/PlanFeatures/Subscriptions/PaymentMethods/Invoices/InvoiceItems/Transactions (migraciones + Eloquent) | Done (schema only, sin integración de pasarela de pago) |
| 7c | Modules/PlanModule/TenantModules (migraciones + Eloquent) | Done |
| 7d | FormFieldDefinitions/Forms/FormFields/Contacts/ContactActivities (migraciones + Eloquent, cifrado en Contact) | Done |
| 7e | Dominio y seguridad de Forms + Contacts: `ContactSubmissionService`, `ContactFormSubmitted` (Mail), `ContactResource`, `ContactPolicy`, `DataMasker` | Done — ver ADR-015 |
| 7f | Re-esquematización de Filament Resources Console (Link/Properties schemas, 12 blocks, slide condicionales, destino menú) | Done |
| 7g | Ocultado del idioma (lang_iso) en UI del Console + Corrección de upload disk | Done |
| 7h | Ajustes y optimización de recursos (MediaSelect modal — superseded por 7i, 3 imágenes responsivas, sliders enums, bloque HEADING, SEO/OG segmentado) | Done |
| 7i | `MediaUpload`: subida directa con preview real (imagen/video) reemplaza `MediaSelect` (dropdown+modal) en los 24 campos de media de Console (Slider/Page/Post), sin cambiar la tabla `media` ni el contrato de la API | Done — ver ADR-028 |
| 8 | API REST `/api/v1` (pages, menus, sliders, posts, media, forms/submit) | Done — ver ADR-016 |
| 9 | Seed Cliente 0 (tenant CICA360, dominio, owner, suscripción, módulos, settings) | Done — ver ADR-014 |
| 9c | Seed de contenido mínimo real de CICA360 (5 páginas + bloques, menú principal, form de contacto, CTAs de slides → páginas reales) | Done — ver ADR-017 |
| 9d | Seed de posts de prueba de CICA360 (3 posts publicados) | Done — ver ADR-018 |
| 13 | Seguridad API por tokens (Sanctum): guard, abilities, ownership de tenant | Done — ver ADR-018 |
| 14 | Console: API Tokens (crear/revocar, plaintext una sola vez, masking) | Done — ver ADR-018 |
| 15b | Console: API Playground (custom Filament Page, requests reales) | Done — ver ADR-018 |
| 16b | Documentación API (`docs/api/v1.md`, `docs/api/openapi.v1.yaml`, Console: API Documentation) | Done — ver ADR-018 |
| 17b | Contrato público de responses sin ids internos (links → source_slug/href, hero → slider_slug, properties/content/meta siempre objeto) | Done — ver ADR-018 |
| 17c | Cierre de gaps: ids anidados en content.items[] (features/testimonials/logos/services_grid) + expiración configurable de tokens | Done — ver ADR-019 |
| 17d | Fix de middleware `abilities` faltante en `bootstrap/app.php` (detectado en el primer `php artisan test` real con Sanctum instalado) + fix de test de revocación (`forgetGuards()`) | Done |
| 17e | Rediseño visual de ApiPlayground/ApiDocumentation/ApiTokens (CSS plano sin build step, alineado a los tokens/patrones reales de Filament) | Done |
| 18 | Dominios base del monolito (`api`/`console`/`manager`) centralizados desde `.env` vía `config/genesis.php` | Done — ver ADR-020 |
| 19 | Preferencias de usuario (idioma/zona horaria) + `FriendlyDate` (fechas amigables/abreviadas) aplicado a `ApiTokens` + fix de `last_four`/`expires_at` no visibles | Done — ver ADR-021. **Requiere `php artisan migrate` local (migración nueva de `users`)** |
| 19b | Extender `FriendlyDate` a las demás columnas de fecha de Console (Pages/Posts/Media/Contacts/etc.) | Pending |
| 19c | Menú del avatar: opciones "Preferencias" y "Cambiar contraseña" (`ChangePassword` page + `Panel::userMenuItems()`) | Done — ver ADR-021 |
| 19d | Fix link roto a `openapi.v1.yaml` (404) en API Documentation (ruta pública nueva + reescritura del href) | Done |
| 19e | Limpieza de lenguaje interno en `docs/api/v1.md`/`openapi.v1.yaml` (sin "MVP", sin referencias a ADRs/docs internos — son client-facing, los ve el desarrollador del tenant) + fix de inconsistencia real de `/api` prefix en ejemplos | Done |
| 20 | Fix: 401 de `/api/*` filtraba `route('login')` (422 con "Route [login] not defined.") cuando el request no mandaba `Accept: application/json` — `bootstrap/app.php` (`redirectGuestsTo`) + envelope 401 distingue sin-token/token-inválido + `errors.code` + 3 tests nuevos en `ApiAuthTest` | Done — ver ADR-023. **Requiere `php artisan test` local (no se pudo correr desde el sandbox)** |
| 19f | Landing page pública tipo tarjeta de presentación en `genesisly.host` | Done |
| 21 | Envelope de error API formalizado: taxonomía `errors.code`, `errors.fields` (validación), `ErrorEnvelope` compartido (`ApiResponds`/handler global), `MissingRequiredFieldsException` en forms/submit, tests nuevos (`FormSubmissionApiTest`) | Done — ver ADR-024. **Requiere `php artisan test` local (no se pudo correr desde el sandbox)** |
| 9b | Seeders de catálogo: Plan Free + features, Módulos core + plan_module, Form field definitions del sistema, Super-admin de plataforma | Done — ver ADR-014 |
| 10 | Tests de aislamiento multi-tenant (incluir nuevos módulos) | Done — `ContactSubmissionServiceTest`, `TenantIsolationTest` y `Api/V1/{PageApiTest,ApiAuthTest,PostApiTest}` cubren aislamiento tenant + auth. `php artisan test` confirmado en verde: **26/26** por el humano el 2026-08-17 (tras instalar Sanctum y corregir el alias de middleware `abilities`, ver ADR-020) |
| 11 | Correr `php artisan migrate` contra la BD real y validar el esquema de ADR-013 | Done (confirmado por el humano: 25 migraciones corridas) |
| 11b | Correr `php artisan db:seed` y `php artisan test` contra la BD real y validar seeders (ADR-014) + tests de Contacts (ADR-015) | Done (todos los seeders corrieron y la suite pasa al 100%) |
| 12 | Auth avanzado del DBML final: roles/permissions tenant-aware, Passport, Sanctum, `social_accounts`, migración `users`↔`tenants` a `tenant_user` | Pending — requiere ADR propio antes de tocar `HasTenant`/`User` (ver ADR-013) |
| 22 | Auditoría y consolidación del ecosistema de dominios/rutas/seguridad/excepciones (código vs. docs) + fix de bug real de CORS (`config/cors.php` seguía en `api/*` tras ADR-025) | Done — ver PROGRESS.md 2026-08-20 |

### P2 — Frontend Cliente 0 (**único foco**)

Contrato mínimo a cablear (Bearer `content:read` / `forms:submit`):

| Método | Path |
|--------|------|
| GET | `/v1/cica360/pages` |
| GET | `/v1/cica360/pages/{slug}` |
| GET | `/v1/cica360/menus/menu-principal` |
| GET | `/v1/cica360/sliders/home` |
| POST | `/v1/cica360/forms/contacto/submit` |

> **Actualizado 2026-08-24**: esta sección quedó desactualizada — el frontend se está construyendo en un repo separado (`/Users/edu/Storage/webapps/cica/cica360/`, ver ADR-003 de ese repo) con su propio `docs/context/`, no acá. No duplicar detalle: la fuente de verdad del estado real del front es `cica360/docs/context/CURRENT_STATE.md` y `TASK.md`. Resumen mínimo para no dejar esta tabla mintiendo:

| # | Subtarea | Estado |
|---|----------|--------|
| 11 | Elegir Astro vs Next (documentar ADR) | Done — Astro SSG + islands (ver `cica360/docs/context/DECISIONS.md` ADR-001) |
| 12 | Scaffold frontend + fetch de los 5 endpoints | Done — `npm run build` confirmado en verde por el humano |
| 13 | Templates de páginas/bloques (home + internas) | En progreso — Hero Slider implementado, resto de bloques son stubs pendientes de diseño visual |
| 14 | Static export + guía de deploy shared hosting | Parcial — CI de build listo, falta confirmar credenciales FTP/SFTP del hosting real |

### P3 — Cierre MVP

| # | Subtarea | Estado |
|---|----------|--------|
| 15 | Hardening básico (auth API, rate limit, published-only) | Done — ver ADR-018 (auth Sanctum + abilities; rate limit ya estaba de ADR-016; published-only ya estaba de ADR-016) |
| 16 | Actualizar docs de contexto y README | Done |
| 17 | Checklist de aceptación firmado en `PROGRESS.md` | Pending |

---

## Definition of Done (sesión de trabajo)

Una subtarea solo se marca **Done** si:

1. El código/docs necesarios están en el repo.
2. Se puede verificar localmente (comando o test).
3. `CURRENT_STATE.md` y este `TASK.md` reflejan el cambio.
4. Se añadió una línea en `PROGRESS.md`.

---

## Handoff

Al soltar la tarea:

```
Owner actual: (libre)
Última subtarea completada (2026-09-02): **Bloques `colophon`/`footer_bottom` (exclusivos de Content tipo `Footer`) + fondo sólido/degradado genérico en `PropertiesSchema`** — ver ADR-040. Pedido del Tech Lead con captura de referencia: "agregar un nuevo bloque en el tipo footer... 'Colophon'... hasta 4 columnas... con un botón dropdown para elegir unos subbloques (lista de links, links de redes sociales con ícono predeterminado, imagen con link)... properties básicos: ancho de contenido, tipo color de fondo (sólido o gradiente)..." + "un último bloque para el pie de página, que permita tener 2 casillas input para colocar el copyright y otro texto opcionales... si es uno al centro si son 2 se posicionan a los costados" + mid-turn: "que cualquier sección donde se requiera tenga opción a gradiente" + "en el seeder va solo colores sólidos predefinidos". Implementado: `SocialPlatformEnum` nuevo (7 plataformas); `PropertiesSchema` gana `background_type`/`background_color_secondary`/`gradient_direction` genéricos (reusables por cualquier bloque, no solo `colophon`); `PageResource.php` gana 2 `Builder\Block` restringidos a Content tipo `Footer` vía `$footerOnlyBlocks` (`colophon`: `Repeater` de hasta 4 columnas con `Builder` anidado de sub-bloques; `footer_bottom`: 2 `TextInput` opcionales); `ResolvesPublicLinks.php` gana `collectLinkIds()` (helper compartido) + resolución de `colophon` (3 niveles: columna → sub-bloque → sus ids) + query batched de `Post` nueva en `attachResolvedBlockContent()`; cica360 gana `src/lib/background.ts` (`resolveBackgroundStyle()`, genérico), `Colophon.astro`/`FooterBottom.astro`, `BlockRenderer.astro`/`types.ts` actualizados; `Cliente0ContentSeeder.php` siembra ambos bloques en `footer-principal` (3 columnas, colores sólidos únicamente).
Siguiente subtarea recomendada: el Tech Lead debe correr `php artisan db:seed --class=Cliente0ContentSeeder` (idempotente) y `npm run build`/`dev` en cica360, y confirmar en Studio: (a) `colophon`/`footer_bottom` SOLO aparecen como opción en Content tipo `Footer`, nunca en Página/Landing/Legal; (b) el Builder anidado de cada columna ofrece los 3 sub-bloques; (c) el contador de caracteres de "Descripción breve" funciona; (d) `background_type: gradient` en cualquier bloque que lo tenga habilitado muestra el segundo color + dirección y el degradado se ve en el sitio; (e) en el sitio, `footer_bottom` centra el texto con 1 campo cargado y lo separa a los costados con los 2. `vendor/bin/pint --dirty`, `php artisan test`, `npm run build`/`astro check` no se pudieron correr desde este sandbox (sin PHP; el binario nativo de `rolldown` está roto en el entorno de Node) — pendientes.
Notas de subtarea anterior (2026-09-02): columna "Tipo" del listado de Páginas separada como badge propio (`Tables\Columns\TextColumn::make('type')->badge()`), ya no concatenada al slug con " - " en la descripción — pedido del Tech Lead: "el tipo de contenido que está a lado del slug tiene que ser un badge para que no se confunda con el slug y sin guion intermedio".
Notas de subtarea anterior (2026-09-01): **Fix real: `SQLSTATE[23502]` `lang_iso` NOT NULL al crear contenido — preset completo en los 7 botones de creación**. Reporte del Tech Lead con captura de Studio (500 al guardar "Crear Aviso Legal", justo después del fix de los botones de estado vacío) + pedido explícito: "revisar que desde ese botón en el listado tanto para página, legal y secciones se preseteen los valores requeridos completos para evitar problemas". Causa: los `CreateAction` del dropdown "Crear Contenido" y de `emptyStateActions()` solo forzaban `type` vía `mutateFormDataUsing()` — `lang_iso`/`status` dependían de que los defaults del FORM (`Hidden`/`Select`) sobrevivieran el `->fillForm()` de un `CreateAction`, y `lang_iso` llegaba `null` explícito al insert (pisa el default de columna en Postgres, que solo aplica si la clave está AUSENTE del insert). Mismo bug latente en los 7 puntos de entrada. Fix: `lang_iso`/`status` forzados explícitos en `fillForm()` + `mutateFormDataUsing()` (con `?? default` para no pisar cambios manuales del usuario) en los 4 `CreateAction` de `ManagePages::getHeaderActions()` y los 3 de `PageResource::emptyStateActions()`; agregado el import faltante de `LanguageEnum` en `PageResource.php` (causaba `Class "App\Filament\Resources\LanguageEnum" not found` — bug propio de esta misma vuelta, detectado con captura en vivo del Tech Lead antes de terminar el fix). Balance de paréntesis/llaves/corchetes verificado con strip manual de comentarios/strings (el conteo bruto daba falso positivo por los `(`/`)` sueltos en comentarios en español). De paso, 3 ajustes menores pedidos en vivo sobre capturas: label del bloque `footer` simplificado de "Footer (referencia a sección compartida)" a "Footer"; campo "Orden" de `logos` ya no es `->required()` (mantiene `->default('first')`); ícono `is_home` de la tabla restringido a Página/Landing (`IS_HOME_ELIGIBLE_TYPES`) — el FORM ya lo restringía, la tabla no.
Siguiente subtarea recomendada: el Tech Lead debe confirmar en Studio que "Crear Página"/"Crear Cabecera"/"Crear Pie de página"/"Crear Aviso Legal" (dropdown "Crear Contenido") y "Crear Página"/"Crear Aviso Legal"/"Crear Sección" (estado vacío de cada tab) guardan sin error de `lang_iso`, y que los 3 ajustes menores (label `footer`, "Orden" opcional en `logos`, ícono `is_home` restringido) se ven bien. `vendor/bin/pint --dirty` y `php artisan test` no se pudieron correr desde este sandbox (sin PHP) — pendientes. Si todo queda confirmado, volver al foco de fondo de la sesión: frontend CICA360 en shared hosting.
Notas de subtarea anterior (2026-09-01): **`pages`: soft delete + papelera en Studio + prefijo "Slug: " en el listado** — ver ADR-039. Pedido del Tech Lead sobre una captura del listado: "usar prefijo 'Slug: ' para identificar o diferenciarse en algunos casos del mismo título similar, los slugs son únicos por tenant y al ser borrados deberán ser soft-delete... solo no deberían duplicarse con contenido activo y no borrado... que permita forzar borrado permanente o vaciar basurero" + mid-turn: "que permita restaurar o recuperar un contenido de la papelera por si borró accidentalmente". Implementado: migración nueva agrega `deleted_at` a `pages` y reemplaza el `unique(tenant_id, lang_iso, slug)` plano por un ÍNDICE ÚNICO PARCIAL (`WHERE deleted_at IS NULL`, vía `DB::statement()`, sintaxis idéntica en Postgres/SQLite) — así un slug papelereado no bloquea reusarlo, pero dos páginas ACTIVAS con el mismo slug siguen siendo imposibles. `App\Models\Page` gana `SoftDeletes` (el global scope que agrega excluye automáticamente los papelereados de cualquier query normal — validación de slug, resolución pública, el Select del bloque `footer`, etc. — sin tocar nada más ahí). `PageResource` gana: `getEloquentQuery()` sin el `SoftDeletingScope` (para que el filtro pueda mostrar los 3 estados), `Tables\Filters\TrashedFilter`, `RestoreAction`/`ForceDeleteAction` por fila, `RestoreBulkAction`/`ForceDeleteBulkAction` masivas, y una acción de header "Vaciar papelera" que borra TODO lo papelereado del tenant de una vez. La columna "Título" ahora antepone "Slug: " para `Página`/`Landing`/`Legal` (Header/Footer sin cambios). De paso, fix de un bug real reportado en vivo por el Tech Lead con captura del error 500 (`SvgNotFound`): el bloque `footer` (agregado un rato antes en la misma sesión) usaba un ícono de Heroicons que no existe (`heroicon-o-rectangle-bottom`) — corregido a `heroicon-o-rectangle-stack`.
Siguiente subtarea recomendada: el Tech Lead debe correr `php artisan migrate` (nueva migración de soft delete) y confirmar en Studio: (a) papelerear una página y que desaparezca del listado default; (b) crear una página nueva con el MISMO slug que una recién papelereada y que NO choque; (c) el filtro de papelera (3 estados) funciona; (d) "Restaurar" trae de vuelta una fila papelereada; (e) "Vaciar papelera" borra todo lo papelereado tras confirmar; (f) el bloque `footer` ya no tira el 500 de ícono al abrir el tab "Secciones". `vendor/bin/pint --dirty` y `php artisan test` no se pudieron correr desde este sandbox (sin PHP) — pendientes.
Notas de subtarea anterior (2026-09-01): **Bloque `footer` genérico — referencia por página a un Content compartido tipo `Footer`, reemplaza el fetch global de `BaseLayout.astro`**. Pedido del Tech Lead: "en el tipo página o landing... necesitamos un bloque footer que permita seleccionar un contenido de su mismo tenant que sea de tipo footer, para relacionarlo como un grupo de secciones comunes en varias páginas que tengan a ese footer"; confirmado por `AskUserQuestion` que esto REEMPLAZA (no coexiste con) el mecanismo anterior (fetch fijo a `footer-principal` en `BaseLayout.astro`, sembrado un rato antes en la misma sesión). Implementado: `BlockTypeEnum::Footer` nuevo; `Builder\Block::make('footer')` en `PageResource.php` con un `Select` (`content.footer_page_id`, opciones = `Page::query()->where('type', PageTypeEnum::Footer->value)->pluck('title', 'id')`) — disponible para `Página`/`Landing`/etc., pero excluido a propósito del subconjunto permitido para un Content tipo `Footer` (anti-recursión); `ResolvesPublicLinks.php` gana resolución recursiva (`resolveFooterBlocks` como guard anti-recursión, batched query de las páginas de footer referenciadas, `content.footer_page.blocks[]` con la misma forma que `BlockResource`); `Cliente0ContentSeeder.php` reordenado para crear `footer-principal` antes de `home`, más un nuevo helper `appendFooterBlock()` que agrega el bloque `footer` al final de las 5 páginas públicas. Lado frontend (cica360, misma sesión): `BaseLayout.astro`/`Footer.astro` revertidos a su forma anterior (sin fetch propio), nuevo `FooterBlock.astro` + entrada en `BlockRenderer.astro`. De paso, ajuste chico pedido aparte: la columna "Título" de Páginas ya no muestra ningún comentario extra para `Header`/`Footer` (solo el label del tipo, sin "sección compartida, sin URL propia").
Siguiente subtarea recomendada: el Tech Lead debe correr `php artisan db:seed --class=Cliente0ContentSeeder` (idempotente) y confirmar en Studio: (a) un Content tipo `Página`/`Landing` ofrece el bloque "Footer (referencia a sección compartida)" con el Select cargando `footer-principal`; (b) un Content tipo `Footer` NO ofrece ese bloque en su propio picker; (c) la API (`GET /pages/{slug}`) devuelve `footer_page.blocks[]` resuelto para ese bloque. Del lado de cica360, confirmar en `npm run dev`/`build` que el CTA vuelve a aparecer al final de cada página pública. `vendor/bin/pint --dirty` y `php artisan test` no se pudieron correr desde este sandbox (sin PHP) — pendientes.
Notas de subtarea anterior (2026-09-01): **Bloque `cta`: rediseño completo** — pedido del Tech Lead con captura de referencia (franja `cicaindigo` sólida, título+subtítulo centrados, un botón dorado en pill con ícono), "con la experiencia que tienes en mejorar y reorganizar el formulario aplicando mas UX". Antes: `Textarea::make('content.body')` redundante con `subtitle` (eliminado), `LinkSchema::make()` (Repeater multi-enlace para lo que siempre fue un solo botón), 5 properties sueltas sin imagen de fondo. Rediseño: (1) un solo botón opcional — `LinkSchema::makeSingle()`, mismo patrón que `rich_text`/`testimonials`; (2) fondo en capas apilables — `properties.background_color` (base) + `content.background_image_id` (`MediaUpload` opcional, capa INTERMEDIA superpuesta encima del color, no lo reemplaza) + las `media_*` genéricas (blend mode + 6 filtros CSS + opacidad) para mezclarla/filtrarla + `overlay_opacity` (velo negro, primera vez con un consumidor real); (3) `content_width` (`full`/`boxed`/`narrow`) sumado — mismo estándar confirmado por el Tech Lead: la `<section>` SIEMPRE es fullwidth, `content_width` solo condiciona el contenedor interno. Del lado de la API: `ResolvesPublicLinks::BLOCK_MEDIA_FIELDS['cta']` nuevo, resuelve `background_image_id` → `background_image`. `Cliente0ContentSeeder` preseteado con los valores reales de la captura (`background_color: #2D2C4D`, `text_color: #FFFFFF`, `content_width: boxed`, `padding_y: lg`, `show_link: true`, `link_radius: full`, `link_size: lg`, `subtitle` extendido) — todos explícitos, sin depender de fallbacks. Implementación conjunta del lado `cica360`: `Cta.astro` reescrito por completo (ver su propio PROGRESS.md).
Siguiente subtarea recomendada: el Tech Lead debe correr `php artisan db:seed` (o `--class=Cliente0ContentSeeder`), abrir el bloque `cta` en Studio (secciones nuevas: Fondo, Botón, Personalización de estilos) y confirmar en el sitio que la franja calza con la captura de referencia sin tocar nada más.
Notas de subtarea anterior (2026-09-01): **Bloque `logos`: `content.limit`/`content.order` para acotar/ordenar cuántos se comparten con la API** — pedido del Tech Lead ("en el admin solo se deberia indicar cuantos se listaran en el api para poner un limite maximo de logos compartidos con el frontsite y el orden los mas recientes o los primeros"). Se le preguntó explícitamente si ameritaba un módulo propio (tabla + Resource, como `testimonials`) o mantener el `Repeater` inline actual — confirmó mantenerlo ("no tenemos una tabla para los logos o brands... solo lo quieren filtrar los primeros o ultimos sin eliminar un logo, solo que no se considere en el frontsite"). Implementado: 2 campos nuevos en `PageResource.php` (bloque `logos`, Section "Límite compartido con la API") — `content.limit` (numérico opcional, 1–28, sin límite por defecto) y `content.order` (`first`/`recent`, default `first`). El filtro real se aplica en `ResolvesPublicLinks::transformBlockContent()`, sobre `content.items` ya con los `media_id` resueltos: `recent` invierte la lista (sin fecha propia, "más reciente" = el último agregado), después `limit` recorta con `array_slice()`. No borra nada en Studio — el `Repeater` sigue teniendo TODOS los logos cargados, solo se recorta lo que sale hacia la API/sitio público. Sin `content.limit` seteado, cero regresión (se comparten todos, como antes de este campo).
Siguiente subtarea recomendada: el Tech Lead debe abrir el bloque `logos` en Studio, confirmar la nueva Section "Límite compartido con la API", guardar con un límite menor a la cantidad real de logos cargados y confirmar que la API/sitio solo muestra esa cantidad en el orden elegido (primeros vs. últimos/más recientes).
Notas de subtarea anterior (2026-09-01): **Bloque `logos`: nueva property `content_width` (fullwidth/boxed), default fullwidth** — pedido del Tech Lead ("en el admin, en las properties del bloque Logos agregar una propiedad de ancho... por default sea fullwidth"). Se reusó el campo genérico `content_width` de `PropertiesSchema` (mismo que `split`/`rich_text`, ver ADR-032) sumado a `PropertiesSchema::make()` del bloque `logos` en `PageResource.php`. Sin `->default()` a nivel de Filament (afectaría solo bloques nuevos, no los ya sembrados) — el default `full` se resuelve en `Logos.astro` (cica360): nueva `content_width` en `LogosProperties` + mapa `CONTENT_WIDTH_CLASSES` (`full`: fullwidth con `px-6 3md:px-[80px]`, igual criterio que `RichText.astro`; `boxed`: el `max-w-6xl` que ya traía el bloque). Cualquier valor que no sea exactamente `'boxed'` cae a `full`. Verificado con `php_balance.py` (genesis, cuadra sin cambio neto) y `astro_check.py` (cica360, cuadra limpio, TAG STACK vacío). Sin PHP/servidor en este sandbox, pendiente de confirmación visual del Tech Lead.
Siguiente subtarea recomendada: el Tech Lead debe abrir el bloque `logos` en Studio y confirmar el selector "Ancho del contenido" en Personalización de estilos, y en el sitio confirmar que por defecto la franja de logos se ve fullwidth (antes era boxed sin excepción) y que "Caja (Boxed)" vuelve al ancho anterior. Volver al foco de fondo de la sesión: frontend CICA360 en shared hosting.
Notas de subtareas anteriores (2026-09-01): **Cadena de bugs de guardado en Studio cerrada y verificada por el Tech Lead** ("quedó") — dos causas raíz distintas, ambas confirmadas con logs/inspector reales, ver ADR-037 + adenda. (1) `MediaUpload`: `saveRelationshipsUsing` guardaba `content.media_id` en su forma interna cruda (array uuid-keyed) en vez del escalar — fix `unwrapFileUploadState()`. (2) `Slider` (opacidad/brillo/saturación/contraste): mismo patrón de fondo pero en otro componente — `SliderStateCast::get()` hace `floatval($state)`, y como el seeder nunca escribe estas propiedades, el valor crudo es `null` → `floatval(null)` = `0`, pisando el default real (100) sin importar el `->default(100)` configurado en Filament (solo aplica al crear un registro nuevo, no al hidratar datos parciales). El Tech Lead lo detectó con el inspector del navegador: el `<img>` tenía el `src` correcto pero `style="filter: brightness(0%) saturate(0%) ...; opacity: 0"` lo volvía invisible — no era un problema de media, era CSS. Fix: mapa `PageResource::SLIDER_PROPERTY_DEFAULTS` + helper `backfillSliderDefaults()`, aplicado al cargar y al guardar. Ambos fixes verificados juntos con el log real del guardado de confirmación: `media_id` llegó escalar y todas las propiedades de slider en `100`. De paso se detectó y resolvió un problema de entorno: MAMP Pro (el servidor local del Tech Lead) estaba sirviendo código cacheado (opcache) — reiniciar los servidores fue necesario para que los fixes tomaran efecto.
Siguiente subtarea recomendada: ninguna urgente sobre este bug — quedó cerrado y confirmado. Si se agrega un `Slider::make('properties.X')` nuevo en cualquier Resource a futuro, hay que sumarlo a `PageResource::SLIDER_PROPERTY_DEFAULTS` con su default real para no reabrir este patrón. Volver al foco de fondo de la sesión: frontend CICA360 en shared hosting (ver tarea activa arriba). Pendiente de más largo plazo, sin urgencia: el dato legado corrupto original (ids no-escalares, `total:24 scalar:12` de los fixes de `ResolvesPublicLinks`) nunca se identificó a nivel de fila — sigue solo blindado, no limpiado, en la DB real.
Notas de subtareas anteriores (2026-09-01): **Causa raíz confirmada y corregida** (no solo blindada) del reporte crítico "guardo sin cambiar nada y se borra/daña el jsonb" — ver ADR-037. Pedí al Tech Lead reproducir el guardado de nuevo; sus capturas mostraron el widget de `MediaUpload` con la miniatura correcta en el modal (descartando problema de hidratación), pero el sitio público perdió la imagen del bloque `split` Y los logos del bloque `logos` ("Empresas con las que trabajamos" quedó vacío) tras el mismo guardado. Agregué un `Log::info()` temporal en `saveRelationshipsUsing()` — el Tech Lead reprodujo una vez más y el log real confirmó la causa exacta: `content.media_id` llegaba como `{"537a6e80-...-uuid":"4"}` (array, no el escalar `"4"` esperado). Causa: el `$state` que recibe ese closure trae los campos `MediaUpload`/`FileUpload` en su forma interna cruda (keyeada por el UUID interno de Livewire) porque el cast que los limpia normalmente (`FileUploadStateCast::get()`) es parte del camino de deshidratación nativo de Filament (`->relationship()`), que este Builder no usa (usa `saveRelationshipsUsing` manual, necesario para la lógica propia de `tenant_id`/`sort_order`/tipo de `Block`). El array corrupto se guardaba tal cual en el jsonb; `ResolvesPublicLinks` (ya blindado en fixes previos del día) lo descarta por no ser escalar → imagen `null` en el sitio público — pero en Studio el widget la sigue mostrando bien porque esa forma sigue siendo "un archivo válido" para el propio FileUpload. El Tech Lead notó el patrón por primera vez trabajando sobre el bloque de partners/logos, pero el bug es genérico a CUALQUIER guardado con campos `MediaUpload` (split, logos, features, services_grid, heading, hero manual) — como se resguardan todos los bloques de la página juntos, un solo "Guardar cambios" en Home corrompió `split` y `logos` a la vez. Fix: helper nuevo `PageResource::unwrapFileUploadState()` (recursivo, detecta la firma exacta por regex de UUID para no tocar objetos legítimos de una sola propiedad como `properties.background_color`), aplicado a `content`/`properties`/`links` de cada bloque antes de guardar. Como la corrupción existente en DB tiene la misma forma que el fix normaliza, el próximo guardado de una fila ya corrupta se autorepara. Log de diagnóstico retirado tras confirmar la causa. El guard de "estado vacío" de la subtarea anterior se mantiene (protección distinta, sigue siendo válida).
Siguiente subtarea recomendada: el Tech Lead debe (1) correr `php artisan db:seed` para partir de datos limpios en los bloques que quedaron corruptos durante las pruebas de hoy (Split 3/4 de Home, `logos`); (2) abrir Home en Studio, guardar sin cambios, y confirmar en el sitio público que la imagen del split y los logos de partners sobreviven; (3) revisar también el fondo de la sección de testimonios que reportó como afectado — ese campo es `properties.background_color` (ColorPicker, no `MediaUpload`), así que no está cubierto por la causa confirmada acá; si sigue roto después del `db:seed`, es un bug distinto y hay que diagnosticarlo de cero. Si todo lo anterior queda bien, dar por cerrada esta cadena de bugs de guardado y volver al foco de fondo de la sesión (frontend CICA360 en shared hosting, ver tarea activa arriba).
Notas de subtareas anteriores (2026-09-01): Fix real y distinto de los 4 anteriores — `content.body` salía como JSON TipTap crudo en vez de HTML, mostrando literalmente `[object Object]` en el sitio real (screenshot del Tech Lead, home ya sin 500). Causa: `Forms\Components\RichEditor::make('content.body')` en `PageResource.php` (usado en los bloques `rich_text`, `split`, `legal_notice`, y en `answer` de cada item de `faq`) guarda el contenido como documento TipTap/ProseMirror JSON — comportamiento de Filament 5, no un error de configuración — pero `RichText.astro`/`Split.astro` siempre esperaron `content.body` como string HTML listo para `set:html`. Nunca existió una conversión entre ambos — bug preexistente que recién se pudo ver una vez que la home dejó de tirar 500 (los 4 fixes anteriores de la sesión). Fix: helper nuevo `renderRichContent()` en `ResolvesPublicLinks.php`, usando `Filament\Forms\Components\RichEditor\RichContentRenderer` — el conversor OFICIAL de Filament (basado en `ueberdosis/tiptap-php`, ya presente en `vendor/`, no hubo que instalar nada) que convierte el JSON a HTML sanitizado (`Str::sanitizeHtml()`, protege contra XSS). Si el valor ya es un string (`cta` usa `Textarea` plano, no `RichEditor`), se devuelve sin tocar. Aplicado a `content.body` de cualquier tipo de bloque (genérico) y a `faq.items[].answer`.
Siguiente subtarea recomendada: el Tech Lead debe recargar la home y confirmar que "¿Qué hacemos?"/"¿A quién nos dirigimos?" (y cualquier `rich_text`/`split`/`legal_notice`/`faq`) muestran el texto real en vez de `[object Object]`, y que el HTML generado (negritas, listas) se ve bien con las clases `.richtext-body` ya definidas en el frontend. Con este fix, los 5 problemas reales encontrados hoy en `ResolvesPublicLinks.php` (array_unique blindado, Collection::get() blindado, content.items reindexado, links reindexado, content.body convertido a HTML) deberían dejar el sitio completamente funcional — si algo TODAVÍA falla después de esto, es un bug nuevo, no una continuación de esta cadena.
Notas de subtareas anteriores (2026-09-01): Tercera y cuarta vuelta del fix de `ResolvesPublicLinks` (mismo dato legado con keys no-secuenciales de las 2 vueltas anteriores, 2 síntomas más). Con los primeros 2 fixes la home de `cica360` ya devolvió **200** por primera vez en esta sesión (confirmado por el Tech Lead: `00:29:07 [200] / 602ms`), pero un segundo después salió `(content.items ?? []).filter is not a function` en `Logos.astro:47`, y luego `block.links.map is not a function` en `Cta.astro:22`. Causa en los dos casos: un array PHP con keys no-secuenciales (mismo dato corrupto) se serializa como objeto JS (`{}`) en vez de array (`[]`) — `array_map()` (para `content.items` de features/logos/services_grid) y `Collection::map()->all()` (para `attachResolvedLinks()`) preservan las keys originales tal cual. Fix: `array_values()` envolviendo esos `array_map()` + una red de seguridad general al final de `transformBlockContent()` (cualquier `content.items`, incluido `faq` que antes pasaba sin tocar, sale reindexado), y `->values()` antes de `->all()` en `attachResolvedLinks()`. En paralelo, `Logos.astro` (cica360) suma `Array.isArray(content.items) ? content.items : []` como defensa en profundidad del lado del frontend, independiente del fix del backend.
Siguiente subtarea recomendada: el Tech Lead debe recargar la home una vez más y confirmar que la terminal de Astro y `laravel.log` quedan sin ningún error. Con los 4 fixes de hoy (array_unique blindado, Collection::get() blindado, content.items reindexado, links reindexado) cualquier forma de array no-secuencial que tenga el dato legado en DB ya no debería poder romper ningún response — el dato corrupto de origen sigue existiendo en la DB real (nunca se identificó la fila/bloque exacto, sin acceso a Postgres desde este sandbox en toda la sesión), pero ya no tiene forma de tirar 500 ni TypeError. Si algo TODAVÍA falla después de esto, ya no es este patrón — hay que diagnosticar el error puntual de cero, probablemente sí necesitando acceso directo a la DB para encontrar la fila exacta.
Notas de subtareas anteriores (2026-09-01): Segunda vuelta del fix de `ResolvesPublicLinks` — el Tech Lead probó el fix anterior reiniciando `npm run dev` y compartió la terminal: el `Array to string conversion` original ya no aparece (confirmado en `laravel.log`), pero salió un error nuevo en el mismo minuto de prueba: `TypeError: array_key_exists(): Argument #1 ($key) must be a valid array offset type at vendor/.../Collection.php:496`. Mismo dato corrupto (el mismo id con forma de array de la vuelta anterior), síntoma distinto: `uniqueScalarIds()` ya protegía el `whereIn()` que arma la Collection `$media`/`$pages`/`$sliders`, pero `resolveMediaRef()` y otros 4 lookups puntuales (`transformPublicLink()` para page/post, `transformBlockContent()` para el slider de `hero` y el `page_id` de `services_grid`) seguían pasando el id CRUDO directo a `->get()`, que internamente hace `array_key_exists($key, ...)` — no tolera `$key` array. Fix: helper nuevo `scalarOrNull()` (`is_scalar($value) ? $value : null`) envolviendo esos 5 `->get()`. Confirmado que `Collection::get()` maneja `null` sano (`$key ??= ''`).
Siguiente subtarea recomendada: el Tech Lead debe reiniciar `npm run dev` una vez más y confirmar que la terminal de Astro ya no muestra ningún `ApiError`/`TypeError` al pedir la home, y que `storage/logs/laravel.log` no suma nuevas entradas de error para ese request. Si TODAVÍA aparece algo, el próximo paso ya no es blindar más código — es encontrar el dato corrupto de raíz en Postgres directamente (`SELECT id, type, content FROM blocks WHERE page_id = (SELECT id FROM pages WHERE slug='home' AND tenant_id=...)`, buscando a simple vista qué `_id` tiene forma de array) y regrabarlo bien desde Studio — este sandbox no tiene conexión a esa DB, esa consulta la corre el Tech Lead.
Notas de subtareas anteriores (2026-09-01): Fix real (primera vuelta) de un 500 en `GET /pages/home` de `cica360` (screenshot del Tech Lead: pantalla de error de Astro, `ApiError`, "Error interno. Intentá de nuevo." — el mensaje fijo que `bootstrap/app.php` devuelve para cualquier 500). Diagnosticado leyendo `storage/logs/laravel.log` REAL (archivo del proyecto en la Mac del Tech Lead, accesible desde este sandbox aunque no haya PHP para ejecutarlo): `ErrorException: Array to string conversion at app/Http/Concerns/ResolvesPublicLinks.php:225`, dentro de `array_unique($mediaIds)` en `attachResolvedBlockContent()`, 9 ocurrencias hoy ~05:20, todas para `page=home`. Causa: algún id (`media_id`/`page_id`/`slider_id`) dentro de un bloque de la home YA guardado en la DB real es un array, no un escalar — `array_unique()` no tolera eso. Repasé `Cliente0ContentSeeder`: todos los ids que siembra son escalares (`Cliente0MediaSeeder::mediaId()` devuelve `?int`), así que el dato corrupto es contenido preexistente en la DB real, no producido por el seeder ni por código que yo haya tocado hoy — y no pude identificar la fila exacta porque este sandbox no tiene conexión a Postgres del Tech Lead. Decisión: blindar en vez de perseguir — nuevo helper `uniqueScalarIds()` en `ResolvesPublicLinks.php` filtra valores no-escalares antes de cada `array_unique()` (5 lugares: `mediaIds`/`pageIds`/`sliderIds` en `attachResolvedBlockContent()`, `pageIds`/`postIds` en `attachResolvedLinks()`) y logea un `Log::warning()` con un label por origen (`content.media`, `content.page`, etc.) cuando descarta algo — el 500 desaparece, pero el dato corrupto de origen sigue sin limpiarse, solo se ignora de forma segura.
Siguiente subtarea recomendada: el Tech Lead debe (1) recargar `/` en `cica360` y confirmar que el 500 ya no aparece; (2) revisar `storage/logs/laravel.log` después de esa recarga — si aparece el nuevo warning `ResolvesPublicLinks: se descartaron ids no-escalares...`, el label indica qué bloque/link tiene el dato corrupto (ej. `content.media` → revisar `media_id` de algún item de `features`/`logos`/`services_grid`/`heading`/`image`/`split` en Studio, guardarlo de nuevo con el valor correcto). Sin eso, el warning queda ahí pero el dato roto sigue existiendo, solo invisible para el usuario final.
Notas de subtareas anteriores (2026-09-01): Regla "Repeaters collapsed by default" aplicada de verdad. El Tech Lead ya había pedido esto ("los sections dentro de cualquier repeat tiene que ser collapsed by default") en una vuelta anterior — audité buscando `Section::make()` anidado dentro de un `Repeater` y no encontré ninguno, reporté "nada que corregir hoy". Lectura incompleta: el Tech Lead volvió a señalarlo con una captura del bloque `logos` (items "Empresa asociada 1/2" expandidos por default) — el problema real eran los `Repeater`s mismos con `->collapsible()` pero sin `->collapsed()`. Corregidos todos los que tenían ese gap: `logos`/`features`/`faq`/`services_grid` (`PageResource.php`), `LinkSchema::make()` (Repeater compartido por CTA/Hero manual/testimonials/etc. — un solo fix cubre todos sus usos), `ServiceResource.php` (Repeaters de "¿Qué ofrecemos?" y "Coberturas"). `MenuResource`/`SliderResource` ya cumplían de antes, no se tocaron. Dejados sin tocar a propósito: `Section::make(...)->collapsible()` de nivel superior que no están anidados dentro de un Repeater (ej. "Diseño de la sección" del Hero, "¿Por qué elegirnos?"/"Tip de ayuda" de Services) — la regla es sobre repeats.
Siguiente subtarea recomendada: el Tech Lead debe confirmar en Studio que los Repeaters tocados arrancan colapsados. Queda como estándar sitewide para cualquier `Repeater` nuevo: `->collapsible()->collapsed()` salvo pedido explícito en contra — vale la pena anotarlo en `ARCHITECTURE.md` o donde se documenten convenciones de Filament si se formaliza más adelante (no hay `.ai/rules`/`record-rule` disponible en este entorno para fijarlo de forma más durable).
Notas de subtareas anteriores (2026-09-01): Fix real de un 500 reportado por el Tech Lead en `/cica360/pages` (`Illuminate\Database\QueryException`, `SQLSTATE[22P02]: invalid input syntax for type integer`, Postgres rechazando un UUID en la columna `sort_order` de `blocks`). Causa: `PageResource.php`, `saveRelationshipsUsing` del `Builder` de bloques — `foreach ($state as $index => $blockData) { ... 'sort_order' => $index }` usaba la key del array de estado como posición, pero el `Builder` de Filament 5 (a diferencia de `Repeater`) keyea su estado por el ID interno de Livewire de cada item (string tipo UUID), no por una posición secuencial. Mientras esas keys "parecían" enteros chicos el bug quedaba invisible; en cuanto Livewire le asignó a un item una key con forma de UUID real, se rompió. Fix de una línea: `foreach (array_values($state) as $index => $blockData)` — reindexa 0,1,2... preservando el orden real en pantalla. Único lugar de la app con este patrón (confirmado por grep sobre todos los Resources).
Siguiente subtarea recomendada: el Tech Lead debe confirmar en Studio que editar/reordenar/duplicar bloques de `/cica360/pages` ya no tira el 500 — probar puntualmente drag-and-drop de reorden (el caso más propenso a generar keys "no numéricas") y guardar. Este sandbox no tiene PHP — no se pudo reproducir el error real ni correr el fix, solo revisión de código + balance de sintaxis.
Notas de subtareas anteriores (2026-09-01): Bloque `logos` ampliado de 7 a 10 placeholders ("generar 10 logos de partners de ejemplo", pedido explícito). 3 archivos nuevos (`cica360_media_logo_8/9/10.png`, 128×128 transparente, generados con Python/Pillow en el mismo estilo que los 7 existentes — badge translúcido + glifo abstracto + subrayado, sin nombre de marca real) commiteados a `storage/app/public/media/`. `Cliente0MediaSeeder::FILES` y `Cliente0ContentSeeder` (bloque `logos` de la home) extendidos a 10 entradas. Con 10 > 7, `Logos.astro` ahora pagina de verdad (7 + 3) — primera vez que el modo carousel del bloque se prueba con datos reales, no solo por revisión de código. Sin cambios de schema/properties/`maxItems` (10 sigue bajo el tope de 28).
Siguiente subtarea recomendada: correr en local `php artisan db:seed --class=Cliente0MediaSeeder --class=Cliente0ContentSeeder` (idempotente, `firstOrCreate`/`updateOrCreate`) y confirmar visualmente en `/` que el carousel de logos pagina 7+3 con flechas/dots/drag. Este sandbox no tiene intérprete PHP (`php -l`/`php artisan` no disponibles) — la única verificación posible fue balance de llaves/paréntesis/corchetes comment-aware sobre los 2 seeders tocados.
Notas de subtareas anteriores (2026-08-31): Bloque `logos` ("Empresas con las que trabajamos") — nunca mostraba nada real en el sitio (5 items placeholder con `media_id: null`). `Cliente0MediaSeeder` gana 7 archivos ya commiteados (`cica360_media_logo_1..7.png`); `Cliente0ContentSeeder` los referencia + agrega `subtitle` + `properties.media_filter_grayscale`/`media_opacity` (ambas properties YA existían — genéricas, mismo set que usa `split` — no se inventó nada nuevo). `PageResource.php`: el bloque `logos` gana una Section "Personalización de estilos" (2 columnas) con esas 2 properties, y el `Repeater::make('content.items')` gana `->maxItems(28)` (4 páginas de 7) como respuesta explícita a "cuánto máximo lista el api" — es un Repeater autocontenido (no una tabla resuelta en runtime como `testimonials`), así que el tope es de UX, no de query. Además, refinamientos varios sobre `testimonials` en esta misma vuelta: `item_background_opacity` (Slider 0-100%, +20 automático al hover) sumado a `item_background_color`, Section "Personalización de estilos" pasada a 2 columnas, `parent_id` fusionado al Grid de 3 columnas en el tab Configuración de `PageResource`, y el bloque testimonials del `LinkSchema` ("Enlace Ver más") pasado de 3 a 2 columnas. El frontend completo (carousel responsive de testimonios, drag+touch, dots; carousel paginado de logos con hover a color real) se implementó en `cica360` — ver su `PROGRESS.md`.
Siguiente subtarea recomendada: correr en local (este sandbox no tiene PHP): `php artisan db:seed` o específicamente `--class=Cliente0MediaSeeder --class=Cliente0ContentSeeder --class=Cliente0TestimonialsSeeder` (siembra los 7 logos, aplica colores/subtítulo/properties nuevas, y blanquea `role` en los 12 testimonios demo — todo `firstOrCreate`/`updateOrCreate`, seguro re-ejecutar), `vendor/bin/pint --dirty --format agent`, `php artisan test`. Confirmar visualmente en Studio: bloque `logos` con la nueva Section de estilos (grayscale/opacidad), tope de 28 items en el Repeater; bloque `testimonials` con Personalización de estilos a 2 columnas. Auditoría pendiente resuelta esta vuelta (sin cambios de código): el pedido "los sections dentro de cualquier repeat tienen que ser collapsed by default" — repasados todos los `Repeater::make()` de la app (`ServiceResource`, `PageResource` ×4, `MenuResource` ×2, `SliderResource`, `LinkSchema`) y ninguno anida hoy un `Section::make()` (usan `Fieldset`, que no tiene collapse) — no hay nada que corregir hoy, pero la regla aplica a partir de ahora para cualquier `Section` nueva dentro de un `Repeater`.
Notas de subtareas anteriores (2026-08-31): Bloque `testimonials` — corrección "expectativa vs realidad" contra 2 capturas del Tech Lead (mockup vs. lo real en CICA360). Property `item_background_color` en `PropertiesSchema`/bloque `testimonials`, colores reales del Design System (`cicagreen-500`/`cicagreen-400`) + subtítulo sembrado en la home. Confirmado sin necesitar cambios: pretitle/title/subtitle ya eran editables en el admin, el CTA "Más casos de éxito" ya apuntaba a la página real, la API ya respetaba el `limit`/`order` por bloque.
Notas de subtareas anteriores (2026-08-31): Árbol de `pages` hasta 3 niveles — ver ADR-036. Pedido del Tech Lead ("como podemos hacer para armar arbol tree de navegacion hasta en 3 niveles, cuando una pagina tenga parent a otra pagina"), reusando la UI de jerarquía de `MenuResource`. Confirmado explícitamente con el Tech Lead (`AskUserQuestion`) que es **solo organización interna en Studio** — la URL pública de cada página sigue siendo su `slug` plano, sin tocar `ResolvesPublicLinks`, la API pública ni la unicidad de slug. Migración nueva `parent_id` autorreferenciado (nullable, `nullOnDelete()`) en `pages`; `Page::parent()`/`children()`/`depth()` nuevos; `PageResource` gana `Select::make('parent_id')` (tab Configuración, `options()` filtrado para evitar ciclos y limitar a 3 niveles) y la tabla indenta visualmente por profundidad (título con eager-load `parent.parent`, orden raíz-primero, `->sortable()` retirado de esa columna).
Notas de subtareas anteriores (2026-08-31, mismo día): fusión de columnas en varios listados (Testimonios, Blog, Páginas: título+slug(+tipo) bajo `->description()`); `PostResource` probó slug-como-link con dominio real del tenant (`Tenant::publicUrl()`, nuevo, no usado por ahora) y se revirtió a texto plano a pedido explícito del Tech Lead; `PageResource.is_home` y `SliderResource.is_active` reescritos de ícono rojo-X/verde-check a un único check clickeable (gris/verde), con fix de paso de un gap de integridad (el toggle de tabla de `is_home` ahora desactiva cualquier otro home del tenant al activar uno nuevo — el `Toggle` del FORM en `HeadingFieldset` todavía no tiene esa protección, gap conocido); `FriendlyDate::ABSOLUTE_FORMAT` fijado a `d:m:Y h:i a`, extendido de `ApiTokens` a `PageResource`/`PostResource`; `MenuResource` — jerarquía de 3 niveles con drag-to-sort propio por nivel. Antes: fix real de un 500 en `/services` (`Service::sanitizeCountries()` + migración de normalización) y `Cliente0ServicesSeeder` ampliado de 7 a 12.
Siguiente subtarea recomendada: correr en local (este sandbox no tiene PHP): `php artisan migrate` (aplica `parent_id` en `pages`, y la normalización de `countries` si no se corrió aún), `vendor/bin/pint --dirty --format agent`, `php artisan test`. Confirmar en Studio: (a) `PageResource` — el nuevo campo "Página superior" aparece en el tab Configuración, al elegir un padre para una página y guardar el listado la muestra indentada debajo de su padre, y no se puede crear un 4to nivel (la página de profundidad 2 no aparece como opción elegible); (b) revisar los 5 servicios nuevos sin captura de mockup en `/services`; (c) confirmar visualmente los toggles de ícono (`is_home`/`is_active`) y el formato de fecha `d:m:Y h:i a` en los listados. Fuera de esta vuelta a propósito: exponer `services`/la jerarquía de `pages` en la API pública, sistema de tags genérico por tenant, protección de "un solo home" en el form (`HeadingFieldset`).
Notas de subtareas anteriores (2026-08-31): `CountryEnum` rediseñado a listado ISO 3166-1 completo — ver ADR-035 (supersede parcialmente ADR-034, módulo de Servicios). Módulo de Testimonios/Casos de Éxito — ver ADR-033. Detalle técnico completo en `DECISIONS.md`/`PROGRESS.md`.
Notas: El estado real de dominios/subdominios/rutas/seguridad/excepciones sigue documentado de forma autoritativa en ARCHITECTURE.md §4 — sin cambios ahí en esta vuelta. Ver ADR-028 en DECISIONS.md para el detalle completo del mecanismo de `MediaUpload`, reusado por `ServiceResource`/`TestimonialResource`.
```
