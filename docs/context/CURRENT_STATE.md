# Genesis CMS — Estado actual

> Fuente de verdad del **dónde estamos**. Actualizar al inicio y al final de cada sesión de trabajo significativa.
> Última actualización: 2026-09-02 (**Mejoras de UX/UI en el módulo de Contenidos**: Títulos dinámicos de SlideOver según tipo de contenido (*Crear Página*, *Crear Aviso Legal*, *Editar Cabecera (Header)*, etc.), tabs de filtrado (*Páginas*, *Legales*, *Secciones*), botones dinámicos de estado vacío per-tab, footer sticky de SlideOver con `z-index: 50 !important` para no solaparse con sliders/builders, y ocultamiento total de `url_preview` en partials (headers/footers). Tests pasando 100% (43/43).)servicios**. Antes: `CountryEnum` rediseñado — ver ADR-035: de 6 países fijos de CICA360 a listado ISO 3166-1 completo (249 + `Global`), `countries` ya no requerido. Supersede parcialmente ADR-034 (módulo de **Servicios**: tabla `services` + `ServiceResource`). Mismo día, antes: módulo propio de **Testimonios/Casos de Éxito** — ver ADR-033. Ver PROGRESS.md para el detalle completo de todos y de la sesión previa del mismo día (split fullwidth, filtros de imagen, fix orden `DatabaseSeeder`, i18n de validación).

---

## Resumen ejecutivo

| Campo | Valor |
|-------|--------|
| Fase | Producto API **cerrado**. Siguiente: frontend CICA360 en shared hosting |
| Versión del producto | 0.0.0 (sin release) |
| Modo de entrega Cliente 0 | Headless |
| Plan Cliente 0 | Free Forever |
| Salud general | 🟢 REST v1 + Sanctum + envelope + seeds CICA360 + playground + hosts **Stamless** listos |

---

## Qué está hecho

- [x] Definición del nombre y visión del producto: **Genesis CMS**
- [x] Decisiones de stack base (Laravel + Filament + PostgreSQL)
- [x] Decisión multi-tenancy: single DB + `tenant_id`
- [x] Decisión de assets: Cloudflare R2
- [x] Modelo de negocio freemium + módulos verticales (dirección)
- [x] Identificación de Cliente 0 (consultora uruguaya seguros/jurídico)
- [x] Estructura de contexto del proyecto para agentes (`docs/context/`, `AGENTS.md`, etc.)
- [x] Scaffold completo de Laravel 13 + Filament 5 + PostgreSQL schema 'genesis' en la base de datos limpia `genesis_cms`
- [x] Migraciones base y modelos de Tenancy (tenants, domains, users) estructurados con ID/UUID/Slug
- [x] Sistema de resolución de tenant y aislamiento automático por trait y global scope
- [x] Tabla de Settings tenant-aware con helper global `setting()` y caché optimizada
- [x] Arquitectura de subdominios y dos paneles Filament (`PanelCmsProvider` en `console` y `PanelManagerProvider` en `manager`)
- [x] Seeders de plataforma (super-admin, Cliente 0 y configuraciones) creados y ejecutados
- [x] Tests unitarios y de integración (10 tests, 36 aserciones) pasando con 100% de éxito y compatibilidad total SQLite/Postgres
- [x] Esquema final de base de datos (contenido + negocio) implementado con migraciones y modelos Eloquent: `media`, `pages`, `blocks`, `posts`, `sliders`, `slides`, `menus`, `menu_items`, `plans`, `plan_features`, `subscriptions`, `payment_methods`, `invoices`, `invoice_items`, `transactions`, `modules`, `plan_module`, `tenant_modules`, `form_field_definitions`, `forms`, `form_fields`, `contacts`, `contact_activities` — ver ADR-013
- [x] 18 Enums PHP nativos (`App\Enums\*`) para catálogos estáticos, implementando `Filament\Support\Contracts\HasLabel`
- [x] `tenants` extendido con `short_hash`, `is_active`, `plan`; `settings` extendido con `type`
- [x] 25 migraciones de ADR-013 corridas exitosamente contra PostgreSQL real (confirmado por el humano)
- [x] Seeders mínimos idempotentes del MVP (`PlanSeeder`, `ModuleSeeder`, `FormFieldDefinitionSeeder`, `PlatformSeeder`, `Cliente0Seeder`) — ver ADR-014. Nombre real de Cliente 0 confirmado: **CICA360**
- [x] Dominio y seguridad de Forms + Contacts — ver ADR-015: `ContactSubmissionService` (guarda + cifra selectivamente por `FormField.is_encrypted`), `ContactFormSubmitted` (Mailable Markdown, best-effort), `ContactResource` (whitelist, nunca sensible), `ContactPolicy` (autorización tenant-aware), `DataMasker` (enmascarado reutilizable), tests de cifrado/aislamiento en `tests/Feature/ContactSubmissionServiceTest.php`
- [x] Re-esquematización completa de recursos de Filament Console para cumplir las especificaciones del Tech Lead (Links estructurados anidados, propiedades tipadas con `PropertiesSchema`, maquetador `Builder` con 12 bloques, fondos responsivos desktop/tablet/mobile en carruseles y resolución dinámica de elementos de menú). Todos los 10 tests integrados son ejecutados de manera exitosa localmente.
- [x] Ocultado del idioma (`lang_iso`) en la interfaz de Console (formularios, builders, tablas y filtros), inyectándose internamente como `'es'`.
- [x] Resolución del disco de carga de multimedia: el campo `disk` en base de datos se guarda dinámicamente con el valor del disco utilizado (`public` en local/desarrollo), y el disco `public` utiliza una URL relativa `/storage` que resuelve fallos CORS/de previsualización infinita.
- [x] Corrección de reactividad y Enums de Filament 5 en `SliderResource` y `MenuResource`, normalizando la hidratación de los objetos Enum PHP.
- [x] Refactorización de `PostResource` y `PageResource` con carga responsiva (Desktop/Tablet/Mobile). Componente reusable `MediaUpload` (2026-08-30, ver ADR-028): subida directa con preview real de imagen/video, reemplaza al viejo `MediaSelect` (dropdown+modal) — sigue creando/leyendo registros de la tabla `media` centralizada, mismas columnas FK.
- [x] Implementación del nuevo bloque `heading` en el constructor de páginas (`PageResource`), agregando soporte completo para 3 imágenes responsivas y propiedades visuales detalladas (alineación, opacidad, brillo, contraste, decorador).
- [x] Reestructuración del formulario SEO/OG en `PageResource` y `PostResource` dividiendo el posicionamiento en Metadata SEO y Open Graph (redes sociales) con cargador de imagen rectangular y cuadrada.
- [x] API REST pública v1 (Headless MVP) — ver ADR-016: `GET /v1/{tenant_slug}/pages`, `pages/{slug}`, `posts`, `posts/{slug}`, `menus/{slug}`, `sliders/{slug}`, `media/{uuid}`, `POST forms/{slug}/submit` (reutiliza `ContactSubmissionService` de ADR-015). Envoltura JSON estándar (ADR-009), tenant resuelto explícitamente por controller (trait `ResolvesTenant`), CORS activo, rate limiting (`api` 60/min, `forms` 10/min), excepciones de `api/*` normalizadas a la envoltura, solo contenido publicado/activo, sin N+1 (blocks/slides/menu items/media eager-loaded). Tests de aislamiento tenant + page-by-slug en `tests/Feature/Api/V1/PageApiTest.php`.
- [x] Contenido mínimo real de CICA360 — ver ADR-017: `Cliente0ContentSeeder` (5 páginas publicadas con bloques — home, sobre-cica, servicios, casos-de-exito, contacto —, menú principal de 5 items, formulario "Contacto principal" con 4 campos) + `Cliente0HomeSlidesSeeder` actualizado para que los CTA de las 3 slides del home resuelvan a páginas reales. Contenido y schema de `content`/`links` compatibles 1:1 con los Resources de Filament (`LinkSchema`, claves `content.*` por tipo de bloque). Sin media real todavía (todos los `*_id` de imagen quedan `null`).
- [x] Seguridad API por tokens + Playground + Docs + Contrato público — ver ADR-018: toda `/v1/{tenant_slug}/*` exige `Authorization: Bearer {token}` (Laravel Sanctum, abilities `content:read`/`forms:submit`); token de otro tenant o sin ability → 403; sin token/revocado → 401. Console gana grupo **Desarrolladores** con 3 páginas nuevas: `ApiTokens` (crear/revocar, plaintext visible una sola vez, masking, expiración configurable), `ApiPlayground` (arma y ejecuta requests reales contra la API del entorno, genera tokens de prueba de 24hs, copia response/cURL), `ApiDocumentation` (renderiza `docs/v1.md`). Contrato público corregido: `links[].source_id` → `source_slug`+`href`, `hero.content.slider_id` → `content.slider_slug`, `properties`/`content`/`meta` vacíos siempre `{}` (nunca `[]`) — vía `ResolvesPublicLinks` + `NormalizesJsonFields`. `Cliente0PostsSeeder` agrega 3 posts publicados de prueba. Docs profesionales en `docs/v1.md` + `docs/api/openapi.v1.yaml`. **Requiere `composer install` local** (se agregó `laravel/sanctum` a `composer.json`, no instalable desde este sandbox).
- [x] Cierre de gaps de ADR-018 — ver ADR-019: `ResolvesPublicLinks::attachResolvedBlockContent()` ahora resuelve TODOS los `*_id` internos de `content` (no solo el hero), incluyendo los anidados dentro de `content.items[]` de `features`/`testimonials`/`logos`/`services_grid` (`image_id`/`avatar_id`/`media_id` → objeto Media; `page_id` → `page_slug`+`href`), y el modo `manual` de `hero`/`heading`/`image`/`split`. `ApiTokens` gana selector de expiración (Nunca/1/30/90/365 días); Sanctum rechaza automáticamente tokens vencidos. Test nuevo cubriendo `services_grid` con ambos ids simultáneos.
- [x] **`laravel/sanctum` instalado y verificado en local por el humano**: `composer require laravel/sanctum:^4.0` (el `composer install` inicial no alcanzaba, el `.lock` no tenía la dependencia nueva), `php artisan migrate`, `php artisan db:seed` corrieron en verde. `php artisan test` expuso un bug real (no de entorno): faltaba registrar el alias de middleware `abilities`/`ability` de Sanctum en `bootstrap/app.php` (Laravel 11+ sin `Http/Kernel.php` no lo auto-registra) — corregido. Un segundo fallo (`ApiAuthTest::test_revoked_token_returns_401_on_subsequent_requests`) era un artefacto del test (el `RequestGuard` de Sanctum cachea el usuario resuelto entre las dos requests HTTP simuladas del mismo test) — corregido con `$this->app['auth']->forgetGuards()`. **Suite completa: 26/26 tests en verde.**
- [x] Rediseño visual de `ApiPlayground`/`ApiDocumentation`/`ApiTokens`: CSS plano hand-authored (`public/css/filament/api-console.css`, sin Tailwind/build step) que consume las variables CSS en tiempo de ejecución del propio Filament (`--primary-*`, `--gray-*`, `--success-*`, etc.) y calca sus patrones reales (`.fi-section`, `.fi-badge`, `.fi-tabs`) — sidebar de endpoints con badges de método coloreados, tabs Pretty/Raw/Headers, syntax highlighting de JSON, TOC con scrollspy en Documentation.
- [x] Dominios base del monolito centralizados desde `.env` — ver ADR-020: `config/genesis.php` (`urls.api`/`urls.console`/`urls.manager`, leídos de `APP_URL_API`/`APP_URL_CONSOLE`/`APP_URL_MANAGER`). `PanelCmsProvider`/`PanelManagerProvider` ya no hardcodean el dominio; `ApiPlayground`/`api-documentation.blade.php` ya no usan `config('app.url')` (dominio de la landing) para armar/mostrar la URL del API.
- [x] Preferencias de usuario (idioma/zona horaria) + fechas amigables — ver ADR-021: `users.locale`/`users.timezone` (migración nueva, requiere `php artisan migrate`), página `App\Filament\Pages\Preferences` (grupo **Cuenta**), helper `App\Support\FriendlyDate` (relativo natural bajo 28 días, absoluto abreviado tipo `17 Ago 12:25 am` en caso contrario, siempre en la timezone/locale del usuario). Aplicado a las 3 columnas de fecha de `ApiTokens`. De paso, dos bugs reales corregidos en esa tabla: `last_four` nunca se guardaba (mass-assignment silenciosa de Sanctum, ahora `forceFill()`) y `expires_at` nunca mostraba "Nunca" (faltaba `->placeholder()` explícito). **Pendiente**: extender `FriendlyDate` a las demás columnas de fecha de Console (Pages/Posts/Media/Contacts, etc.) — solo se cubrió `ApiTokens` en esta vuelta.
- [x] Fix de bug real de CSS en **API Documentation** (modo oscuro): los bloques de código de ejemplo (fences ```json de `docs/v1.md`) mostraban un "sombreado" por línea que parecía texto seleccionado. Causa: `html.dark .gnss-prose code` (estilo de código inline, con fondo/padding/radius) tenía más especificidad CSS que el reset pensado para código dentro de `<pre>` (`.gnss-prose pre code { background: none }`), así que en modo oscuro ganaba igual — y al ser `<code>` un elemento inline envolviendo contenido multilínea, el navegador renderizaba ese fondo como una caja redondeada separada por línea. Fix: `html.dark .gnss-prose pre code { background: none; }` con la misma especificidad para neutralizarlo específicamente.
- [x] Limpieza de lenguaje interno en `docs/v1.md` y `docs/api/openapi.v1.yaml` (los docs client-facing que ven los desarrolladores del tenant en **API Documentation**): sacadas todas las menciones a "MVP", referencias a `ADR-XXX`, y links a documentos internos de gestión (`CURRENT_STATE.md`). La sección "Pendientes conocidos (honestidad ante todo)" se renombró a "Limitaciones actuales" y se le sacaron los ítems que revelaban roadmap/bloqueadores internos (ej. "bloqueador de Cloudflare R2", "pendiente como su propio paso") — quedan solo limitaciones técnicas reales, sin lenguaje de "esto es una prueba". De paso se corrigió una inconsistencia real: los ejemplos de cURL/fetch/`servers` del OpenAPI usaban `.../v1/...` sin el prefijo `/api`, mientras que la sección "Base URL" (y las rutas reales) sí lo llevan — unificado a `.../v1/...` en todos lados.
- [x] Fix: link roto a `openapi.v1.yaml` en **API Documentation** (404). El markdown fuente (`docs/v1.md`) usa un link relativo `./openapi.v1.yaml` que tiene sentido en el repo, pero al renderizarse dentro de `App\Filament\Pages\ApiDocumentation` (anidado bajo `/{tenant}/api-documentation`) el browser lo resolvía como `/{tenant}/openapi.v1.yaml`, que no existe. Fix: nueva ruta pública `GET /openapi.v1.yaml` en `routes/web.php` (dominio Console, sin auth — es un contrato de API público, no datos de tenant) que sirve el archivo crudo con `Content-Type: application/yaml`; `ApiDocumentation::getMarkdownHtml()` reescribe ese href relativo a la ruta real vía `route('docs.openapi-yaml')`.
- [x] Menú del avatar con "Preferencias" y "Cambiar contraseña" — ver ADR-021: `App\Filament\Pages\ChangePassword` (page nueva, oculta del sidebar — `$shouldRegisterNavigation = false` —, solo accesible desde el dropdown del avatar; campos `current_password`/`password`/`password_confirmation`, valida contraseña actual con `->currentPassword()`, guarda vía cast `hashed` de `User`). `PanelCmsProvider::panel()` gana `->userMenuItems([...])` con dos `Filament\Navigation\MenuItem` (Preferencias → `Preferences::getUrl()`, Cambiar contraseña → `ChangePassword::getUrl()`), sumados a los ítems nativos de Filament (toggle de tema, Salir). `Preferences` ahora también tiene `$shouldRegisterNavigation = false` (se sacó del sidebar/grupo "Cuenta" a pedido del humano, para no duplicarla con el dropdown). No verificado todavía en browser real por el humano.
- [x] Landing page pública tipo teaser en `stamless.host` (título Stamless, footer dinámico desde `config('app.url')`, tagline "Una fuente. Todos los sitios.").
- [x] Ocultamiento de la pantalla de bienvenida de Laravel en subdominios: la raíz de `api.genesisly.host` ahora retorna una respuesta `404` completamente vacía (`response('', 404)`), implementándose un endpoint público `/v1/health` (sin prefijo `/api` — ver ADR-025) para pings de monitoreo y chequeo de base de datos. Cualquier otra visita no mapeada redirige de manera segura a la landing page principal (`genesisly.host`), y cualquier intento de acceder a `/api/*` o `/graphql/*` en el dominio principal (`genesisly.host`) o a `/graphql/*` en el de la API (`api.genesisly.host`) es capturado para retornar una respuesta `404` vacía.
- [x] Fix de bug real: `GET /api/*` sin `Authorization` y sin header `Accept: application/json` devolvía `422` con `"message":"Route [login] not defined."` en vez de `401` — ver ADR-023. Causa: Laravel registra por defecto `redirectGuestsTo(fn () => route('login'))`, y esta app headless nunca definió esa ruta; cuando `expectsJson()` da `false`, `Authenticate::unauthenticated()` intenta resolverla y explota antes de construir la `AuthenticationException` real. Fix en `bootstrap/app.php`: `redirectGuestsTo` devuelve `null` para `/api/*` sin importar el header Accept.
- [x] Envelope de error API formalizado — ver ADR-024 (extiende ADR-023): taxonomía completa `errors.code` snake_case por tipo de excepción (`unauthenticated`/`token_invalid`/`forbidden`/`not_found`/`validation`/`too_many_requests`/`server_error`), `errors.fields` para validación (`{campo: [mensajes]}`), `errors.detail` en `500` solo si `APP_DEBUG=true` (nunca stack trace en producción). Nueva clase estática `App\Support\Api\ErrorEnvelope` — único lugar que arma el JSON de error; `App\Http\Concerns\ApiResponds::error()` delega ahí (ya no duplica el array) y el handler global de `bootstrap/app.php` la usa directo. Nueva excepción `App\Exceptions\Api\MissingRequiredFieldsException` (extiende `InvalidArgumentException` a propósito, compatible con `ContactSubmissionServiceTest` existente) que le da a `POST forms/{slug}/submit` un desglose real por campo en vez de un string libre. Tests: 3 nuevos/reforzados en `ApiAuthTest`/`PageApiTest` con `errors.code`, más `tests/Feature/Api/V1/FormSubmissionApiTest.php` (nuevo — antes `forms/submit` no tenía cobertura a nivel API, solo de servicio). **No se pudo correr `php artisan test` en este sandbox (sin PHP) — pendiente de confirmación del humano.**
- [x] **No-hardcoded domains — regla de proyecto:** todos los dominios y subdominios del código ejecutable eliminados y reemplazados por `config('app.url')` / `config('genesis.urls.*')`, que leen de `APP_URL` / `APP_URL_API` / `APP_URL_CONSOLE` / `APP_URL_MANAGER` en `.env`. Archivos corregidos: `routes/web.php`, `config/cors.php`, `resources/views/public/home.blade.php`, `database/seeders/Cliente0Seeder.php`, `tests/Feature/ExampleTest.php`, `tests/TestCase.php`. Quedan menciones solo en comentarios y docs. **Esta regla es vinculante para todo agente futuro: ningún dominio concreto puede aparecer en código ejecutable fuera del `.env`.**
- [x] `composer dev` configurado para excluir el proceso `server` (`DevCommands::except('server')` en `AppServiceProvider::boot()`) — MAMP PRO ya sirve la app; correr ambos causaba conflicto de puerto.
- [x] **Regenerar API Tokens (Sanctum) en Console** — ver ADR-018: nueva acción "Regenerar" en `App\Filament\Pages\ApiTokens` (junto a "Revocar"), que borra el token actual y crea uno nuevo con el mismo `name`/`abilities`/`expires_at` (o pide una nueva expiración si el token ya había vencido), mostrando el plaintext una sola vez en el mismo banner que usa "Crear token"; scoping estricto por `tokenable_id === auth()->id()` (403 si no es el dueño, 404 si el token es de otro tenant vía el query ya existente de la tabla).
- [x] **Auditoría y consolidación del ecosistema de dominios/rutas/seguridad/excepciones** (código vs. docs, sin asumir nada — ver `PROGRESS.md` para el detalle completo): confirmado que la migración a `Route::domain()` + `apiPrefix: ''` (ADR-025) está completa y consistente en `routes/api.php`/`routes/web.php`/`bootstrap/app.php`/tests. Se encontró y corrigió un **bug funcional real**: `config/cors.php` seguía con `'paths' => ['api/*']` tras adoptar ADR-025, por lo que CORS había dejado de aplicarse a la API en silencio (el matching es por path, no por Host) — corregido a `'v1/*'`. Se corrigieron además 5 inconsistencias de documentación (URL de health check con `/api` de más en `ARCHITECTURE.md`, comentario stale en `routes/api.php`, ejemplos de error 403/422 desactualizados en `docs/api/v1.md`/`openapi.v1.yaml` que no reflejaban el formato fijo de ADR-024, y contenido obsoleto en `ARCHITECTURE.md` §2/§9 de una versión pre-Sanctum/pre-Laravel-13). `ARCHITECTURE.md` §4 ahora es la referencia autoritativa y enlaza explícitamente ADR-012/016/018/020/023/024/025.
- [x] `MenuItem.is_home` resuelto por el backend desde `Page.is_home` (2026-08-30) — `MenuController::attachResolvedHrefs()` batch-resuelve `is_home` junto con `href`; expuesto en `MenuItemResource`. Consumido por el front (cica360 Header) para excluir el link "Home" del navbar sin comparar `href === '/'` a mano. Test nuevo `MenuApiTest`.
- [x] **Slide: posición/alineación de contenido, decorador inferior configurable, efectos visuales de fondo** — ver ADR-027: columna `description` eliminada de `slides`; 15 campos nuevos (`position_container`, `align_content`, `decorator_bottom`/`_color`/`_opacity`, `slide_background_color`/`_brightness`/`_opacity`/`_blend_mode`/6 filtros) agregados como claves de `properties` jsonb (no columnas dedicadas), vía 4 Enums PHP nuevos (`PositionContainerEnum`, `AlignContentEnum`, `DecoratorShapeEnum`, `BlendModeEnum`) y extensión de `App\Filament\Schemas\PropertiesSchema`. `SliderResource.php` gana tab "Decorador y Efectos". `Cliente0HomeSlidesSeeder` sembra `properties` completo por slide. **Requiere `php artisan migrate` local (nueva migración `2026_08_30_000001_drop_description_from_slides_table`) — no se pudo correr desde este sandbox (sin PHP)**.
- [x] **Bloque `rich_text`: personalización visual completa + enlace único opcional** — ver ADR-029: `PropertiesSchema` gana `show_scroll_indicator`, `show_link`, `decorator_top_opacity` (3 componentes nuevos, de 26 a 29); `PageResource.php` reesquematiza `rich_text` en 2 secciones colapsables (enlace "ver más" único vía `LinkSchema::makeSingle()` gateado por `show_link`; diseño de la sección con fondo/color/alineación/ancho/padding/decoradores arriba+abajo/flecha de scroll). `Cliente0ContentSeeder` siembra `properties` completo en los 4 bloques `rich_text` existentes (home/sobre-cica/servicios/contacto), el de home con `links[0]` real. Frontend (`cica360/src/components/blocks/RichText.astro`) reescrito consumiendo todas las properties nuevas — ver `cica360/docs/context/PROGRESS.md`.
- [x] **`split`: `->cloneable()` + duplicado eliminado + contenido real del home + `Split.astro` implementado** — ver ADR-032: `Builder::make('blocks')` gana `->cloneable()` (duplicar bloques en Studio); `properties.media_position` duplicado eliminado (queda solo `content.media_position`); home page ("¿Qué hacemos?" pasó de `Features` a `Split`, "¿A quién nos dirigimos?" completado) sembrada con contenido real + 2 imágenes nuevas vía `Cliente0MediaSeeder`; `Split.astro` (frontend) pasó de stub a componente real con grid de 2 columnas alternando `media_position`.
- [x] **Hero: campos de modo Manual agrupados + duplicado eliminado** — ver ADR-031: `LinkSchema::make`/`PropertiesSchema` (CTA, fondo, color, alineación, padding, overlay, animación, flecha de scroll) se movieron dentro de la Section "Configuración Manual" (un solo `->visible(content.mode === 'manual')` para todo el grupo, ya no visibles en modo Slider); reorganizados en `Grid::make(2)`. Se eliminó un duplicado real: `content.overlay_opacity`/`content.align`, campos muertos que pisaban `properties.overlay_opacity`/`properties.text_align`. Pendiente: `properties.overlay_opacity` sigue sin consumidor en `Hero.astro` (overlay hardcodeado, no dinámico) — fuera de alcance de este cambio.
- [x] **Contenido inicial con media: assets commiteados + `Cliente0MediaSeeder`** — ver ADR-030: `database/seeders/Cliente0MediaSeeder.php` (nuevo) siembra la tabla `media` desde archivos YA commiteados en `storage/app/public/media/` (`cica360_media_slide{1,2,3}.webp`), sin subir nada en runtime; `Cliente0HomeSlidesSeeder` resuelve cada fondo vía `Cliente0MediaSeeder::mediaId()` y asigna el mismo id a `image_desktop_id`/`image_tablet_id`/`image_mobile_id` (un solo fondo por breakpoint por ahora). Patrón establecido como estándar para futuro contenido con media (featured images de pages/posts, etc.), no exclusivo de Slides. **Pendiente que el Tech Lead corra `php artisan db:seed` con los datos ya reseteados y confirme visualmente.**
- [x] **Hero: `show_scroll_indicator` a nivel Slider (no por slide)** — ver actualización de ADR-027: `sliders` gana su primera columna `properties` jsonb (migración `2026_08_31_000002_add_properties_to_sliders_table`, **pendiente correr `php artisan migrate` local**); `SliderResource.php` mueve el toggle del fieldset por-slide "Decorador inferior" a la sección "General" a nivel Slider ("Flecha de scroll"); `PageResource.php` (bloque `hero`) gana el mismo toggle visible solo en modo Manual. `Cliente0Seeder::upsertPlaceholderSlider()` siembra `properties.show_scroll_indicator = true` una sola vez en el Slider `home` (se retiró de `Cliente0HomeSlidesSeeder`). Frontend (`Hero.astro`) calcula la flecha una sola vez fuera del `.map()` de slides — ver `cica360/docs/context/PROGRESS.md`.
- [x] **Fix orden de `DatabaseSeeder` + UX del form `split` + mensajes de validación en español**: `Cliente0MediaSeeder` corría después de `Cliente0ContentSeeder` (bug propio, arrastrado desde la entrada anterior) — los splits sembraban `media_id: null` siempre; reordenado a `Cliente0Seeder → Cliente0MediaSeeder → Cliente0ContentSeeder → Cliente0HomeSlidesSeeder → Cliente0PostsSeeder`. Form del bloque `split` reorganizado (sin celda de grid vacía: `is_visible` + `media_position` llenan la fila pareja, `MediaUpload` pasa a fila propia `columnSpanFull`) y las 5 ocurrencias de `Toggle::make('is_visible')` en `PageResource.php` ganan `helperText` explicando su función (oculta el bloque de la API pública sin borrarlo). `lang/es/validation.php`/`auth.php`/`passwords.php`/`pagination.php` creados (el proyecto nunca tuvo `lang/` propia, caía al inglés embebido en `vendor/laravel/framework` pese a `APP_LOCALE=es`) — mensajes de validación de Laravel core ahora en español; Filament ya traía los suyos. **Pendiente que el Tech Lead corra `php artisan db:seed` (o `migrate:fresh --seed`) y confirme visualmente**; si el locale sigue en inglés, probar `php artisan config:clear` (config cacheada vieja podría pisar el `.env`).
- [x] **`split`: `MediaUpload`+`Encabezado` a 2 columnas + "Personalización de estilos" con filtros/blend de imagen** — ver actualización de ADR-032: `MediaUpload` y `HeadingFieldset` ahora comparten fila (`Grid::make(2)`). Los 4 campos de estilo sueltos (fondo/texto/padding/animación) se reorganizaron en una `Section` colapsable "Personalización de estilos" con 2 `Fieldset` a 2 columnas; el segundo, "Imagen: filtros y efectos", trae 10 campos nuevos (`media_blend_mode`/`media_brightness`/`media_opacity`/`media_radius` + 6 filtros CSS) agregados a `PropertiesSchema` (de 31 a 41 componentes) bajo el prefijo genérico `media_*` (no `slide_background_*`, reusable a futuro por cualquier bloque con `MediaUpload`). `Split.astro` ya los consume (mismo patrón de `filter`/`opacity`/`mix-blend-mode` que `Hero.astro` usa para el fondo del Slide).
- [x] **`split`: `content_width` fullwidth asimétrico + `media_position` reubicado** — ver actualización 2 de ADR-032: "Posición de la imagen" se movió de junto a `is_visible` hacia adentro de "Personalización de estilos" > "Sección" (solo reubicación de UI, sigue siendo `content.media_position`). Se agregó `content_width` (`full`/`boxed`/`narrow`) al mismo Fieldset — a diferencia del `full` simétrico de `rich_text` (80px de padding en ambos lados), en `split` la variante `full` es asimétrica: la imagen llega hasta el borde real del viewport (bleed) y el texto conserva el padding de 80px solo del lado exterior, calculado dinámicamente según de qué lado está la imagen. Implementado en `Split.astro`.
- [x] **Big dot bullets** para `<li>` del cuerpo de texto (`rich_text`/`split`) — simplificación pedida por el Tech Lead sobre un intento previo de selector de ícono por bloque (revertido por completo, ver `PROGRESS.md`): sin properties nuevas, resuelto 100% en CSS (`::before` circular, `currentColor`) en ambos componentes Astro. De paso, ajustes iterativos de espaciado (`<ul>`/`<p>`) y peso/contraste del texto (`font-normal` + `<strong>`/`<b>` en `font-weight: 700` + `color: gray-800` para contraste vía color, no peso).
- [x] **Módulo propio de Testimonios/Casos de Éxito** — ver ADR-033: nueva tabla `testimonials` (`name`/`role`/`quote`/`avatar_id`/`is_visible`/`sort_order`, tenant-scoped) + modelo `Testimonial` + `TestimonialResource` (Filament 5, patrón `ManageRecords`, avatar circular, toggle de visibilidad, filtro, acciones masivas mostrar/ocultar, drag-reorder). El bloque `testimonials` de `PageResource.php` queda reducido a encabezado + filtro (`content.limit`/`content.order`) + enlace único opcional + estilos — sin `Repeater`. `ResolvesPublicLinks::attachResolvedBlockContent()` resuelve la lista en runtime (una sola query batched entre bloques, respeta `is_visible`, entrega la misma forma `content.items[]` que ya consumía el frontend). `Cliente0TestimonialsSeeder` nuevo, ampliado a **12 testimonios de ejemplo** (4 originales + 8 nuevos con roles variados), con `avatar_id` real — las 5 fotos subidas por el Tech Lead (sembradas vía `Cliente0MediaSeeder`) se reutilizan a mano en un orden mezclado (2-3 testimonios por foto). `Testimonials.astro` reescrito: corrige el bug real `TestimonialItem.author`→`name` (los nombres jamás se renderizaban) y reemplaza el stub sin estilos por el diseño del mockup (fondo teal, avatar circular con fallback de iniciales para el caso sin foto, frase/nombre en itálica, botón "ver más" con ícono de ojo). **Actualización**: `TestimonialResource` reorganizado a 2 columnas (avatar + datos) con modal `2xl` (antes se veía angosto/desperdiciado en el slide-over por default). **Pendiente que el Tech Lead corra `php artisan migrate && php artisan db:seed` y confirme visualmente**.
- [x] **Módulo de Servicios** — ver ADR-034: nueva tabla `services` (mismo esqueleto que `pages`: pretitle/title/subtitle/slug/status/meta/links/properties/published_at) + `image_id`/`countries` (jsonb, `App\Enums\CountryEnum`)/`content` (jsonb: `intro`, `offers[]` "¿Qué ofrecemos?", `coverages[]` "Coberturas"/lo que incluye con detalle opcional tipo sub-lista, `why_choose_us`, `tip`) + modelo `Service` + `ServiceResource` (Filament 5, `Tabs` de 3 pestañas — Configuración/Contenido/SEO — mismo patrón que `PageResource`, 2 `Repeater`s en Contenido, tabla con banderas de país como badges, reorder por `sort_order`, modal `6xl`). `HeadingFieldset::make(hasSlug: true)` NO se reutilizó (acoplado a `Page::class`/`PageTypeEnum`) — slug propio vía `afterTitleUpdated` + `unique(Service::class, ...)`. `Cliente0ServicesSeeder` nuevo: **7 servicios** reales (de las 9 cards de la captura del catálogo, 2 eran duplicadas de relleno) — "Seguros generales" con contenido verbatim de la captura del Tech Lead, el resto redactado en el mismo tono a revisar. `image_id` null en los 7 (fotos vienen después). **Fuera de esta vuelta a propósito** (no pedido todavía): exposición pública en la API (`GET /v1/{tenant}/services`), si `services_grid` pasa a consumir esta tabla (mismo tratamiento que `testimonials`), y el componente Astro de catálogo/detalle en `cica360`. **Pendiente que el Tech Lead corra `php artisan migrate && php artisan db:seed` y confirme visualmente**.
- [x] **`CountryEnum`: listado ISO 3166-1 completo (249 países), campo opcional** — ver ADR-035 (supersede parcial de ADR-034): reemplaza los 6 casos fijos de CICA360 por el catálogo ISO 3166-1 alpha-2 completo (nombres en español vía CLDR) + `Global`, valores en MAYÚSCULA (`PE`, `AR`, ...) para calzar con la convención de banderas del frontend `media/flags/flag_{ISO}.webp`. Un `Enum` PHP es un catálogo global compartido por todo el código (no `tenant_id`-scoped) — cubrir todos los países ya lo hace reutilizable por cualquier tenant sin gestión extra. Helpers nuevos `toApiArray()`/`resolveMany()`/`flagPath()` en el Enum y `Service::countriesResolved()` en el modelo, pensados para cuando se construya la API pública de `services`. `ServiceResource`: `countries` ya no `->required()`, `->searchable()` en form y filtro de tabla. La idea alternativa del Tech Lead (tags libres con autocompletado cruzando otros servicios) queda anotada en el ADR como solución para un campo *distinto* — clasificación abierta propia de cada tenant sin catálogo universal — no implementada, sin pedido concreto todavía. **Pendiente que el Tech Lead corra `php artisan db:seed --class=Cliente0ServicesSeeder` de nuevo** (los 7 servicios ya sembrados quedaron con códigos en minúscula que el Enum nuevo no reconoce).
- [x] **Fix 500 real en `ServiceResource` + `Cliente0ServicesSeeder` a 12 servicios** — abrir/editar una fila sembrada antes de ADR-035 rompía con `TypeError` (Filament no tolera un valor de `Select` que no existe entre las `options()` actuales). `Service::sanitizeCountries()` nuevo (mayúscula + descarta códigos inválidos, tolera instancias de `CountryEnum` además de strings) enchufado en el campo `countries` de `ServiceResource` (`afterStateHydrated()`/`dehydrateStateUsing()`) + migración `2026_08_31_000005_normalize_services_countries.php` que limpia los datos ya sembrados directo en Postgres (query builder, no Eloquent — `HasTenant` necesita contexto de tenant). `Cliente0ServicesSeeder`: 7 → **12 servicios** (5 nuevos sin captura: Seguros de Vida y Salud, RRHH y Nómina, Comercio Exterior y Aduanas, Seguros Empresariales, Turismo y Asistencia al Viajero), a revisar por el Tech Lead. Modal de `ServiceResource` reducido de `6xl` a `3xl` (pedido de UX — ocupaba casi toda la pantalla). **Pendiente que el Tech Lead corra `php artisan migrate && php artisan db:seed --class=Cliente0ServicesSeeder` y confirme visualmente**.
- [x] **`MenuResource`: jerarquía de 3 niveles con drag-to-sort propio por nivel** — menú/submenú/sub-submenú, pedido explícito del Tech Lead. El esquema (`parent_id` autorreferenciado en `menu_items`, `Menu::rootItems()`/`MenuItem::children()`) ya soportaba jerarquía real — sin migración. Se reemplazó el `Repeater` plano (ligado a `Menu::items()`, todos los niveles mezclados) + `Select` manual de `parent_id`, por 3 `Repeater`s anidados recursivamente (`MenuResource::menuItemFields(int $depth)`) — nivel 1 ligado a `rootItems()`, niveles 2 y 3 a `children()` del item padre, cada uno con su propio `orderColumn('sort_order')` — el drag-to-sort de un submenú ya no mezcla los demás niveles. La jerarquía ahora es estructural (posición en el Repeater = padre), sin `Select` manual.
- [x] **Fusión de columnas + toggles de ícono + fechas amigables sitewide** (mismo día, sesión de UX de listados): `TestimonialResource` (nombre+rol), `PostResource` (título+URL del slug como texto, sin hipervínculo — se probó como link con dominio real del tenant vía `Tenant::publicUrl()` y se revirtió a texto plano a pedido explícito del Tech Lead) y `PageResource` (título+slug+tipo) fusionan su columna secundaria bajo el título principal vía `->description()`. `PageResource.is_home` y `SliderResource.is_active` reescritos de ícono rojo-X/verde-check a un único heroicon de check clickeable (`->action()` en la columna, gris apagado si está en `false`, verde si está en `true`) — mismo criterio aplicado a **todas** las columnas "Activo" de la app (confirmado por grep, `SliderResource` era la única). De paso, fix de un gap de integridad no pedido: el toggle de tabla de `is_home` ahora desactiva cualquier otra página `is_home=true` del mismo tenant al activar una nueva (el `Toggle` del FORM en `HeadingFieldset` todavía no tiene esa misma protección — gap conocido, no resuelto). `App\Support\FriendlyDate::ABSOLUTE_FORMAT` fijado a `d:m:Y h:i a` (reemplaza el formato relativo abreviado anterior) y extendido de `ApiTokens` a `PageResource`/`PostResource` — ver actualización de ADR-021.
- [x] **Red de seguridad urgente: guardar una página ya no puede borrar todos los bloques por un estado vacío** — reporte real del Tech Lead ("si guardo sin cambiar nada, se daña el json"). `saveRelationshipsUsing` del Builder de `blocks` en `PageResource.php` tenía `$record->blocks()->whereNotIn('id', $existingBlockIds)->delete()` al final sin ningún guard — si el estado del Builder llegaba vacío por cualquier motivo (causa raíz NO confirmada, sin PHP/DB en este sandbox para reproducir), esa línea borraba TODOS los bloques de la página. Fix: si el estado llega vacío y la página ya tiene bloques guardados, se aborta el guardado (se preservan los bloques existentes) y se logea un `Log::warning()`. Pendiente de confirmación real del Tech Lead — esto protege contra el peor caso pero no cierra el diagnóstico de la causa raíz.
- [x] **Fix real: `content.body` salía como JSON TipTap crudo (`[object Object]` en pantalla)** — `Forms\Components\RichEditor::make('content.body')` (usado en `rich_text`/`split`/`legal_notice`/`faq.items[].answer`) guarda un documento TipTap/ProseMirror en JSON, no HTML, pero `RichText.astro`/`Split.astro` siempre esperaron un string de HTML listo para `set:html`. Nunca hubo conversión — bug preexistente, invisible hasta que la home dejó de 500ear. Fix: helper `renderRichContent()` en `ResolvesPublicLinks` usando `Filament\Forms\Components\RichEditor\RichContentRenderer` (conversor oficial de Filament, `ueberdosis/tiptap-php`, ya en `vendor/` — nada nuevo que instalar), que convierte a HTML sanitizado. Strings planos (`cta` usa `Textarea`, no `RichEditor`) pasan sin tocar.
- [x] **Tercera/cuarta vuelta: `content.items` y `block.links` podían salir como objeto JS en vez de array** — con los 2 fixes anteriores la home ya devolvía 200, pero se destaparon 2 síntomas más del mismo dato legado con keys no-secuenciales: `array_map()` en `transformBlockContent()` (items de features/logos/services_grid) y `Collection::map()->all()` en `attachResolvedLinks()` preservan keys — un array PHP con keys no-secuenciales serializa como objeto JS (`{}`), rompiendo `.filter()`/`.map()` del lado del frontend (`Logos.astro`, `Cta.astro`). Fix: `array_values()` en los `array_map()` de items + una red de seguridad general al final de `transformBlockContent()` para cualquier `content.items`, y `->values()` antes de `->all()` en `attachResolvedLinks()`. Además, `Logos.astro` (cica360) suma `Array.isArray()` como defensa en profundidad del lado del frontend.
- [x] **Segunda vuelta: `resolveMediaRef()` y otros 4 lookups puntuales también necesitaban el guard** — el fix de `uniqueScalarIds()` (bullet de abajo) resolvió el `array_unique()`, pero destapó un `TypeError: array_key_exists()` en `Collection::get()`: `resolveMediaRef()`, `transformPublicLink()` (page/post) y `transformBlockContent()` (slider, services_grid page) seguían pasando el id crudo (potencialmente el array corrupto) directo a `->get()`. Nuevo helper `scalarOrNull()` envolviendo los 5 lugares. Diagnosticado con un log real posterior al primer fix — confirma que el primer fix funcionó y aisló este segundo síntoma del mismo dato corrupto.
- [x] **Fix real: 500 en `GET /pages/home` — `ResolvesPublicLinks` blindado contra ids no-escalares** — `array_unique($mediaIds)` en `attachResolvedBlockContent()` tiraba `ErrorException: Array to string conversion` (confirmado en `storage/logs/laravel.log` real, 9 ocurrencias) porque algún `media_id`/`page_id`/`slider_id` ya guardado en un bloque de la home es un array, no un escalar — dato preexistente en DB, no producido por el seeder actual (que solo siembra escalares) ni inspeccionable desde este sandbox (sin Postgres). Nuevo helper `uniqueScalarIds()` filtra no-escalares antes de `array_unique()` en los 5 lugares del trait que arman ids (`attachResolvedBlockContent`/`attachResolvedLinks`) y logea un `Log::warning()` con la ubicación (`content.media`/`content.page`/`content.slider`/`links.page`/`links.post`) para poder rastrear y limpiar el dato de origen más adelante — el 500 desaparece pero el dato corrupto en sí sigue sin corregirse, solo se ignora.
- [x] **Regla "Repeaters collapsed by default" aplicada de verdad** — el Tech Lead pidió "los sections dentro de cualquier repeat tiene que ser collapsed by default"; la primera lectura buscó `Section` anidado dentro de `Repeater` (cero casos, nada que corregir) pero la intención real era que los `Repeater`s mismos arrancaran colapsados. Corregidos todos los `Repeater::make()->collapsible()` sin `->collapsed()`: bloque `logos`/`features`/`faq`/`services_grid` (`PageResource.php`), `LinkSchema::make()` (compartido por CTA/Hero manual/etc.), `ServiceResource.php` (offers/coverages). Estándar desde ahora para cualquier Repeater nuevo.
- [x] **Fix real: 500 al editar/reordenar bloques de una página (`sort_order` recibía un UUID)** — `PageResource.php`, `saveRelationshipsUsing` del Builder de bloques hacía `foreach ($state as $index => $blockData)` y usaba `$index` como `sort_order`; el `Builder` de Filament 5 keyea su estado por el ID interno de Livewire del item (string tipo UUID), no por posición secuencial como `Repeater`. En cuanto esa key dejaba de "parecer" un entero, Postgres rechazaba el UUID en la columna integer (`SQLSTATE[22P02]`). Fix de una línea: `foreach (array_values($state) as $index => $blockData)`. Único lugar de la app con este patrón (confirmado por grep).
- [x] **Bloque `logos`: ampliado de 7 a 10 placeholders para ejercitar el carousel real** — con exactamente 7 items la sección "Empresas con las que trabajamos" solo mostraba la grilla estática de una página; el modo carousel de `Logos.astro` nunca se probaba con datos sembrados. 3 placeholders nuevos (`logo_8/9/10`, mismo estilo visual que los 7 anteriores — generados con Python/Pillow, sin nombre de marca real) sumados a `Cliente0MediaSeeder`/`Cliente0ContentSeeder`. Con 10 > 7, el carousel pagina en 2 páginas (7 + 3) apenas se corra el seeder.
- [x] **Bloque `logos`: 7 logos reales sembrados + filtro grayscale/opacidad + tope explícito de items** — el bloque "Empresas con las que trabajamos" nunca mostraba nada en el sitio (5 placeholders con `media_id: null`, seeder viejo). `Cliente0MediaSeeder` gana los 7 archivos ya commiteados; `Cliente0ContentSeeder` los referencia + agrega `subtitle` + `properties.media_filter_grayscale`/`media_opacity`. Sin properties nuevas: `media_filter_grayscale`/`media_opacity` ya eran genéricas (mismo set de `split`), solo se sumaron al schema del bloque `logos` en `PageResource.php`, agrupadas en su propia Section "Personalización de estilos" a 2 columnas. `Repeater::make('content.items')` gana `->maxItems(28)` (4 páginas de 7) — respuesta a "cuánto máximo lista el api": el bloque es un Repeater autocontenido, no una tabla resuelta en runtime, así que el tope es de UX, no de query. Frontend (carousel paginado de a 7, hover a color real) en `cica360`.
- [x] **Bloque `testimonials`: property `item_background_color` + colores/subtítulo reales sembrados** — corrección "expectativa vs realidad" (2 capturas del Tech Lead: mockup vs. lo real en CICA360). `PropertiesSchema` gana `item_background_color` (fondo por tarjeta, independiente del fondo de la sección) en el schema de "Personalización de estilos" del bloque. Confirmado sin cambios de código: pretitle/title/subtitle ya eran editables en el admin, el botón "Más casos de éxito" ya apuntaba a la página interna real, y la API ya respetaba el `limit`/`order` propio de cada bloque. `Cliente0ContentSeeder` actualizado a los colores reales del Design System CICA360 (`cicagreen-500`/`cicagreen-400`, antes un teal ad-hoc) + subtítulo en el bloque de la home. El resto del rediseño (avatares, tarjetas, carousel táctil) vive en `cica360` — ver su `PROGRESS.md`.
- [x] **Árbol de `pages` hasta 3 niveles (`parent_id`), solo organización en Studio** — ver ADR-036: nueva migración `parent_id` autorreferenciado (nullable, `nullOnDelete()`) en `pages`, mismo patrón ya probado en `menu_items`. `Page::parent()`/`children()`/`depth()` nuevos. `PageResource` gana `Select::make('parent_id')` (tab Configuración) con `options()` filtrado para evitar ciclos y limitar a 3 niveles (excluye la página misma, sus descendientes, y cualquier página ya en profundidad 2); tabla con eager-load `parent.parent`, orden raíz-primero, e indentación visual (`— `) por profundidad en la columna `title` (se le quitó `->sortable()` para no romper ese orden agrupado). Confirmado con el Tech Lead vía pregunta explícita: es **solo organización** — la URL pública de cada página sigue siendo su `slug` plano, sin cambios en `ResolvesPublicLinks`, la API pública, ni la unicidad de slug. **Pendiente que el Tech Lead corra `php artisan migrate`.**
- [x] **Content tipo `Footer` + bloque `footer` genérico (referencia por página a un Content compartido)** — ver ADR-038: `PageTypeEnum::Footer` con picker de bloques del Builder restringido (`image`/`cta`/`features`/`faq`/`contact_form`/`testimonials`/`logos`); Content sembrado `footer-principal` con el CTA que antes vivía hardcodeado en la Home. `BlockTypeEnum::Footer` nuevo + `Builder\Block::make('footer')` en `PageResource.php` (Select a `content.footer_page_id`, disponible en `Página`/`Landing`/etc., no en `Footer` — anti-recursión) permite que cualquier página referencie explícitamente cuál Content de footer usa. `ResolvesPublicLinks` resuelve recursivamente la página referenciada completa (`content.footer_page.blocks[]`, guard `resolveFooterBlocks` anti-recursión). `Cliente0ContentSeeder` agrega el bloque `footer` al final de las 5 páginas públicas (`appendFooterBlock()`). Reemplaza un mecanismo intermedio de fetch global fijo (`getPage('footer-principal')` en `BaseLayout.astro`), descartado explícitamente por el Tech Lead por menos flexible — frontend revertido a su forma anterior + nuevo `FooterBlock.astro`/`BlockRenderer.astro` en cica360.
- [x] **Soft delete en `pages` + papelera en Studio + prefijo "Slug: " en el listado** — ver ADR-039: `deleted_at` nuevo, `SoftDeletes` en `Page`, `unique(tenant_id, lang_iso, slug)` reemplazado por un índice único PARCIAL (`WHERE deleted_at IS NULL`, vía `DB::statement()`, idéntico en Postgres/SQLite) — un slug papelereado no cuenta como existente, pero dos páginas activas con el mismo slug siguen siendo imposibles. `PageResource` gana `getEloquentQuery()` sin `SoftDeletingScope`, `Tables\Filters\TrashedFilter` (3 estados), `RestoreAction`/`ForceDeleteAction` por fila, `RestoreBulkAction`/`ForceDeleteBulkAction` masivas, y una acción de header "Vaciar papelera" que borra TODO lo papelereado del tenant de una vez. La columna "Título" antepone "Slug: " para `Página`/`Landing`/`Legal`. **Pendiente que el Tech Lead corra `php artisan migrate`.**

---

## Qué está en progreso

**Único foco (humano, 2026-08-20):** frontend CICA360 en shared hosting, consumiendo REST v1 con Bearer (`content:read` / `forms:submit`).

- [ ] Frontend Cliente 0 (Astro o Next.js static export) — **siguiente sesión, único trabajo**

**Aparcado hasta que el sitio esté en el aire** (no tocar ahora):

- Filament Resource de Contacts (list/detail)
- Extender `FriendlyDate` al resto de columnas de Console
- Spam-filtering honeypot/reCAPTCHA
- Re-auth para exports masivos de Contacts
- Auth avanzada (roles/permissions, `tenant_user`, social accounts)
- Integración R2
- GraphQL
- Marca fina / polish visual de Stamless (rebrand ejecutado — ADR-026)

---

## Siguiente paso inmediato

**Lado producto (API) cerrado.** No seguir puliendo Console, envelope, hosts ni GraphQL.

Único trabajo al volver: sitio estático de **CICA360** en el shared hosting del cliente, consumiendo:

```
GET  /v1/cica360/pages
GET  /v1/cica360/pages/{slug}
GET  /v1/cica360/menus/menu-principal
GET  /v1/cica360/sliders/home
POST /v1/cica360/forms/contacto/submit
```

Auth: Bearer token con abilities **solo** `content:read` / `forms:submit`. Host API: `config('stamless.urls.api')` (hoy `api.stamless.host`, sin prefijo `/api`).

Primer paso de esa sesión: elegir Astro vs Next.js (static export) y documentar ADR; scaffold + fetch de esos 5 endpoints; home + páginas internas; export HTML/CSS/JS para shared hosting.

Detalle operativo: ver [`TASK.md`](./TASK.md).

---

## Bloqueadores actuales

| Bloqueador | Impacto | Estado |
|------------|---------|--------|
| Credenciales Cloudflare R2 | Media/assets | Pendiente de provisionar cuando se implemente storage |
| Dominio / DNS Cliente 0 | Deploy frontend | No bloquea desarrollo local |
| Elección final Astro vs Next.js | Frontend Cliente 0 | Decisión pendiente (preferencia: Astro por static hosting simple) |

---

## Cliente 0

| Campo | Detalle |
|-------|---------|
| Nombre | **CICA360** (Centro Internacional de Consultoría y Asesoría) |
| Tenant | `slug = cica360` (renombrado del placeholder original `cliente-0` vía `Cliente0Seeder`, ver ADR-014) |
| Tipo | Consultora uruguaya |
| Rubros | Seguros + temas jurídicos |
| Entrega | Headless (API + frontend estático) |
| Hosting frontend | Shared hosting del cliente |
| Plan | Free Forever (seed: plan `free`, suscripción activa) |
| Estado comercial | Cliente 0 / validación de producto |
| Estado técnico | Datos base + contenido mínimo real sembrados (tenant, dominio, owner, suscripción, módulos core, settings, slider home con 3 slides, 5 páginas con bloques, menú principal, form de contacto) — ver ADR-017. Sin media real (R2 pendiente) |
| Contenido esperado (borrador) | Home, servicios, sobre nosotros, contacto, casos de éxito — Done (texto); legales — pendiente |

---

## Entorno de desarrollo

| Ítem | Estado |
|------|--------|
| Repo Git | Activo y con base de código inicializada |
| Laravel | Instalado (Laravel 13) |
| Filament | Instalado (Filament v5.7.6, dos paneles) |
| PostgreSQL | Configurado en el puerto 5434, base de datos 'genesis_cms', schema 'genesis' |
| Tests | Configurados y con 100% de éxito (26 tests, confirmado por el humano en local) |
| CI/CD | N/A |

---

## Notas para el próximo agente

- Leer primero: este archivo, `TASK.md`, `ARCHITECTURE.md` §4 (API/hosts), `DECISIONS.md`.
- **Único foco:** front CICA360 en shared hosting. No reabrir GraphQL, marca fina, Contacts Resource ni polish de Console.
- Contrato a consumir: los 5 endpoints de `/v1/cica360/...` con Bearer `content:read` / `forms:submit`.
- No reinventar multi-tenancy multi-DB; la decisión es **single DB + tenant_id**.
- Antes de handoff: actualizar este archivo + `TASK.md` + entrada en `PROGRESS.md`.
